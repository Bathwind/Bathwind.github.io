<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>VScode配置Python运行环境</title>
    <url>/WYW1314/posts/10987.html</url>
    <content><![CDATA[<h1 id="VScode配置Python运行环境"><a href="#VScode配置Python运行环境" class="headerlink" title="VScode配置Python运行环境"></a>VScode配置Python运行环境</h1><p>最近打算用python做一个东西，但是感觉用Pycharm不是特别舒服把，于是自己就打算用万能的VScode配置下环境，然后去找帖子，发现大部分网上的帖子真的水的一批，找了半天配置Python运行环境的帖子，都长得一样还不好使。本地原本安装python的库都识别不出来。最后自己试了半天才好使，把这个过程记录一下。</p>
<p>首先确认你电脑本地已经安装了Python和VScode。这俩个软件的安装就不在赘述。直接进入正题。</p>
<p>首先进入命令行，先安装俩个插件，分别是flake8和yapf.这俩个插件的作用分别是flake8会检查编写代码时的不规范的地方和语法错误。yapf可以把你写的代码不规范的地方给格式化。</p>
<p>命令分别是：<code>pip install flake</code> 和 <code>pip install yapf</code>.安装完成后进入VScode安装插件：</p>
<p><img src="https://img-blog.csdnimg.cn/20200808104848775.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>这个插件的作用并不是就是Python这个解释器，</p>
<ol>
<li>第一：这个插件不是python语言的解释器，vscode想要运行python代码，必须另外指定在windows上安装的python解释器路径才可以。</li>
<li>第二：这个插件被安装上后，vscode就具有了调用python解释器的功能。其实就是在setting.json和launch.json这两个配置文件中，可以添加python相关的配置了，比如通过python.pythonPath的值找到python解释器的位置等等。</li>
<li>第三：可以通过按下ctrl键，点击函数，打开函数的所在文件和定义位置。</li>
<li>第四：具有代码提示功能。</li>
</ol>
<p>接下来就可以愉快的写代码了。首先创建一个文件夹存放你的程序。这里我新建一个mysocket文件夹存放程序，记得这里一定要以Vscode右键打开文件夹，</p>
<p><img src="https://img-blog.csdnimg.cn/2020080821294839.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>打开之后，ctrl+shift+P调出命令面板，在命令行输入第一行的内容，Python:Select Interpreter.</p>
<p><img src="https://img-blog.csdnimg.cn/20200808213948790.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>接下来就会出来你电脑上所有的python环境，如图所示：<br><img src="https://img-blog.csdnimg.cn/2020080822095490.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"><br>这里选择好你要的环境之后，然后你创建的文件下就会出现一个.vscode文件夹，然后文件夹里面有一个setting.json.这个时候你打开setting.json,这个时候里面只有一句:python的路径设置。<br><img src="https://img-blog.csdnimg.cn/20200808221539556.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"></p>
<p>然后接下来添加几条语句： </p>
<pre class=" language-lang-bash"><code class="language-lang-bash">    "python.linting.flake8Enabled": true,
    "python.formatting.provider": "yapf",
    "python.linting.flake8Args": ["--max-line-length=248"],
    "python.linting.pylintEnabled": false
</code></pre>
<p>以上语句分别是对flake8和yapf的应用。</p>
<p>以上语句分别是对flake8和yapf的应用。之后还要创建一个launch.json来配置输出在哪里。<br><img src="https://img-blog.csdnimg.cn/20200808224506250.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"></p>
<p>点击之后，会出来一个让你选择的窗口，如下图所示：<br><img src="https://img-blog.csdnimg.cn/20200808224655614.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"><br>这里选择python file。然后就会自动生成一个配置。如下图所示：<br><img src="https://img-blog.csdnimg.cn/20200808224834768.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"></p>
<p>然后就可以写程序试验了。这里我随便写一句：<img src="https://img-blog.csdnimg.cn/20200808224914992.png#pic_center" alt="在这里插入图片描述"></p>
<p>然后按F5运行，一般情况底下会自动弹出你运行的结果。<br><img src="https://img-blog.csdnimg.cn/2020080822504546.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"></p>
<p>我们可以看到出现了hello world。但是这里还显示其余和程序运行无关的东西，看起来很碍眼，所以我们可以将程序输出换下地方：<br>在launch.json中将 “console”: “integratedTerminal”换为 “console”:”internalConsole”即可，这时候我们在运行下看看：<br><img src="https://img-blog.csdnimg.cn/20200808225435107.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"><br>到此配置就基本结束了。OK！！！</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>VScode</category>
      </categories>
      <tags>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>Manjoro kde双系统的踩坑之路</title>
    <url>/WYW1314/posts/27692.html</url>
    <content><![CDATA[<h1 id="Manjoro-kde双系统的踩坑之路"><a href="#Manjoro-kde双系统的踩坑之路" class="headerlink" title="Manjoro kde双系统的踩坑之路"></a>Manjoro kde双系统的踩坑之路</h1><p>本来打算是想在虚拟机里面玩玩这个系统，但是觉得虚拟机玩着没那个意思。于是自己就打算搞个双系统。这几天在这里面踩了好多坑。唉，搞到最后，发现其实还不如在windows上，做开发用真的足够了，但是我挺烦这个开机贼慢和进桌面都会黑屏的问题。另外自己，有许多搞嵌入式的在<code>linux</code>上实在没有。所以我打算写完这篇文章后继续回到我的Windows。相信<code>linux</code>系统的未来会更好。略有些遗憾告别了这个系统。</p>
<h2 id="1：下载镜像"><a href="#1：下载镜像" class="headerlink" title="1：下载镜像"></a>1：下载镜像</h2><p>首先第一步，去<a href="https://manjaro.org/" target="_blank" rel="noopener">Manjoro官网</a>下载镜像。镜像的话这里是有三个版本，第一个版本是<code>xfce</code>,第二个版本是<code>kde</code>版本的，第三个gnome.然后我觉得<code>kde</code>版本的比较友好吧。而且功能比较强大，里面我觉得最好的一个软件就是<code>kdeconnect</code>,这个软件真的好用，可惜windows没有类似的软件。</p>
<p><img src="https://img-blog.csdnimg.cn/20200807110844383.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70" alt="image-20200807061733642"></p>
<p><img src="https://img-blog.csdnimg.cn/20200807110844340.png" alt="image-20200807061751738"></p>
<p><img src="https://img-blog.csdnimg.cn/20200807110844241.png" alt="image-20200807061959519"></p>
<p>以上就是三个版本的，下载镜像即可，但是由于服务器在国外的原因，速度很慢，这里我把百度云链接放到这，需要的自取：链接: <a href="https://pan.baidu.com/s/14ywNtYmKfcvZ4jP760WNQA" target="_blank" rel="noopener">https://pan.baidu.com/s/14ywNtYmKfcvZ4jP760WNQA</a> 提取码: <code>fy4d</code> 。</p>
<h2 id="2：分出一个磁盘并且烧录镜像进去"><a href="#2：分出一个磁盘并且烧录镜像进去" class="headerlink" title="2：分出一个磁盘并且烧录镜像进去"></a>2：分出一个磁盘并且烧录镜像进去</h2><p>目前主流的两种启动系统的方式：<br> <code>*legacy*</code>启动+<code>MBR</code>分区表</p>
<p><code>*UEFI*</code>启动+<code>*GPT*</code>分区表</p>
<p>这里我们需要先分出一个磁盘，分盘的操作大家应该都没啥问题，所以这一步直接省略了。右键选择一个磁盘-&gt;属性-&gt;卷，在磁盘分区形式一栏中可以看到是<code>GPT or MBR</code>，这里的话我们要记住了，一会<code>rufus</code>设置的要用的到</p>
<p><img src="https://img-blog.csdnimg.cn/20200807111037579.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807062524813"></p>
<p>烧录镜像的有俩个软件可以选择，一个是<code>rufus</code>，另一个<em><code>win32diskimage</code></em>把，具体我也忘了，<code>rufus</code>下载的链接为：<a href="http://rufus.ie/。打开后是这个页面：" target="_blank" rel="noopener">http://rufus.ie/。打开后是这个页面：</a></p>
<p><img src="https://img-blog.csdnimg.cn/20200807111136758.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807063125027"></p>
<p>这里选择other version,然后括号里面<code>github</code>和<code>fosshub</code>无所谓。下载版本一定选择3,4的。否则<code>gpt</code>分区没法选择。<img src="https://img-blog.csdnimg.cn/20200807111323109.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807063402726"></p>
<p>插入u盘，打开<code>Rufus</code>，我的分区类型一栏选择的是<code>GPT</code>，其他的都为默认，</p>
<p>点击<code>开始</code>后弹出该窗口，点击以<code>DD</code>镜像模式写入：</p>
<p><img src="https://img-blog.csdnimg.cn/20200807124412174.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="https://img2018.cnblogs.com/blog/1539665/202001/1539665-20200109130229425-1414184345.png"></p>
<h2 id="3-配置系统以及安装manjoro"><a href="#3-配置系统以及安装manjoro" class="headerlink" title="3 配置系统以及安装manjoro"></a>3 配置系统以及安装<code>manjoro</code></h2><p>然后正常装系统操作，这个就不写了，之后会一步步进入到安装引导界面。如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/20200807111556583.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807063945798"></p>
<p><code>lang =en_us</code>这一行的话可以设置下语言，其余保持默认即可。然后切到Boot那一行按回车即可。</p>
<p>接下来就回来到下一个界面。</p>
<p><img src="https://img-blog.csdnimg.cn/20200807111708861.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807064119633">在这个界面先连下网，然后点击黄框选中的，开始一步步傻瓜式安装，接下来几幅图就是对应的：</p>
<p><img src="https://img-blog.csdnimg.cn/20200807111757778.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807064349625"></p>
<p><img src="https://img-blog.csdnimg.cn/20200807111810314.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807064421389"></p>
<p><img src="/WYW1314/posts/27692/20200807112151745.png" alt="image-20200807064447679"></p>
<p>分区的话选择自动替换一个分区就可以。记得分区选择我们之前分出那个区域。让<code>manjoro</code>帮我们自动分区。</p>
<p>接下来设置账户信息，这个就不用多说了吧，设置好以后就下一步就行咯。</p>
<p><img src="https://img-blog.csdnimg.cn/20200807111858279.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807064723022"></p>
<p>最后会让你确认一下是否正确。没啥错误就点击安装就好了。<img src="https://img-blog.csdnimg.cn/20200807111918353.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807064828750"></p>
<p>之后等待进度条走完，然后选择重启，拔掉u盘。剩下的就可以开心的配置<code>Manjoro</code>了。</p>
<h2 id="4：调节字体以及安装各种常用软件"><a href="#4：调节字体以及安装各种常用软件" class="headerlink" title="4：调节字体以及安装各种常用软件"></a>4：调节字体以及安装各种常用软件</h2><h3 id="1-换源"><a href="#1-换源" class="headerlink" title="1 换源"></a>1 换源</h3><p>首先需要进行的第一步就是换源。</p>
<p>换源的俩条指令，依次输入命令即可：</p>
<pre class=" language-lang-python"><code class="language-lang-python">sudo pacman-mirrors -i -c China -m rank
sudo pacman -Syy
</code></pre>
<p>第一条指令输入后会显示几个速度快的源，这里都选上即可。然后再输入第二条指令。</p>
<p>之后输入指令 ：<code>sudo nano /etc/pacman.conf</code></p>
<p>在文件最后一行加入：</p>
<pre class=" language-lang-python"><code class="language-lang-python">[archlinuxcn]
SigLevel = Optional TrustedOnly
Server = https://mirrors.ustc.edu.cn/archlinuxcn/$arch
</code></pre>
<p>然后<code>ctrl+x</code>退出，点击Y，回车保存即可。</p>
<p>之后输入指令 ：</p>
<pre><code>sudo pacman -Syy
sudo pacman -S archlinuxcn-keyring
</code></pre><h3 id="2：安装各种常用软件"><a href="#2：安装各种常用软件" class="headerlink" title="2：安装各种常用软件"></a>2：安装各种常用软件</h3><p>首先安装下中文输入法，这里的话搜狗显然是更加好用的，</p>
<p>首先安装<code>yay,</code>这个可以<code>pacman</code>去安装软件：</p>
<pre><code>sudo pacman -S yay base-devel
</code></pre><pre class=" language-lang-python"><code class="language-lang-python">yay -S fcitx-qt4 kcm-fcitx fcitx-sogoupinyin
sudo echo -e "export GTK_IM_MODULE=fcitx\n export QT_IM_MODULE=fcitx\n export XMODIFIERS=@im=fcitx" >>~/.xprofile
</code></pre>
<p>依次输入上面俩条指令，然后<code>reboot</code>重启。</p>
<p>谷歌浏览器：</p>
<pre><code>sudo pacman -S google-chrome
</code></pre><p>安装完后可以把火狐卸了。谷歌安装插件的方法和windows上没有区别，依旧是<code>.crx</code>文件往里面拖就好。</p>
<p><code>git,VScode:</code></p>
<pre><code>sudo pacman -S git
</code></pre><pre><code>sudo pacman -S visual-studio-code-bin
</code></pre><p>网易云音乐：</p>
<pre><code>yay -S netease-cloud-music
</code></pre><p>安装的软件通病都是字体不会跟随系统字体而变化，这一点我觉得很不舒服，还要自己来配置。</p>
<p>安装:<code>tim</code></p>
<p><code>yay -R deepin-wine-tim</code></p>
<p>安装之后可能出现闪退的现象，这时候输入以下指令：</p>
<pre class=" language-lang-python"><code class="language-lang-python">sudo pacman -S gnome-settings-daemon

设置/usr/lib/gsd-xsettings为自启动

系统设置->开机或关机->自动启动->添加脚本->输入/usr/lib/gsd-xsettings

重启即可打开
</code></pre>
<p><code>tim</code>界面也可能会出现字体极小的现象。这时候不要慌，输入以下指令，也就是调节wine的<code>dpi</code>设定:</p>
<pre class=" language-lang-python"><code class="language-lang-python">env WINEPREFIX="$HOME/.deepinwine/Deepin-TIM" winecfg
</code></pre>
<p>设置显示界面的<code>dpi</code>即可，也就是吧<code>dpi</code>的数值调大即可。</p>
<p><img src="https://img-blog.csdnimg.cn/20200807112547714.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807071545604"></p>
<p>之后就显示正常了，very good!!!。</p>
<p>安装截图工具并设置快捷：</p>
<p>这里推荐<code>flameshot</code>.</p>
<p>输入代码：<code>sudo pacman -S flameshot</code></p>
<p>之后在系统设置全局快捷键：</p>
<p><img src="https://img-blog.csdnimg.cn/20200807112637114.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200807071854172"></p>
<p>这里右键选择新建-全局快捷键-命令(<code>URl</code>)。快捷根据自己习惯设置，动作输入：<code>flameshot gui</code> ，然后应用保存即可。</p>
<p>安装<code>typora:</code></p>
<p><code>sudo pacman -S typora</code></p>
<h3 id="3：字体设置："><a href="#3：字体设置：" class="headerlink" title="3：字体设置："></a>3：字体设置：</h3><p>网易云界面字体的设置方法：</p>
<p>修改 <code>/opt/netease/netease-cloud-music</code>下的<code>netease-cloud-music.bash</code></p>
<p>最后一行添加上：</p>
<p><code>--force-device-scale-factor=1.8</code>即可。</p>
<p>然后系统字体我调节的大小大概是14像素：</p>
<p>缩放调节的120%.感觉看的才比较舒服一点。</p>
<h3 id="4-美化界面："><a href="#4-美化界面：" class="headerlink" title="4 美化界面："></a>4 美化界面：</h3><h4 id="1安装latte-dock栏："><a href="#1安装latte-dock栏：" class="headerlink" title="1安装latte-dock栏："></a>1安装latte-dock栏：</h4><p>输入以下指令：</p>
<pre class=" language-lang-python"><code class="language-lang-python">sudo pacman -S latte-dock
</code></pre>
<p>关闭背景大小<br> 绝对大小48<br> 鼠标悬停放大40%<br> 删除模拟时钟<br> 在dock上添加部件<br> 左侧为应用程序面板<br> 右侧为回收站</p>
<p>之后自己配置dock栏就可以了。</p>
<h4 id="2安装zsh"><a href="#2安装zsh" class="headerlink" title="2安装zsh"></a>2安装<code>zsh</code></h4><p>输入下面这条指令安装git.之后会用到git。</p>
<pre class=" language-lang-bash"><code class="language-lang-bash">sudo pacman -S git
</code></pre>
<p>接下来输入以下指令：</p>
<pre class=" language-lang-bash"><code class="language-lang-bash">sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
</code></pre>
<p>如果出现404那就是网络原因，自己可以在桌面建立一个<code>install.sh.</code>内容如下：</p>
<pre class=" language-lang-python"><code class="language-lang-python"> #!/bin/sh
#
# This script should be run via curl:
#   sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
# or wget:
#   sh -c "$(wget -qO- https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
#
# As an alternative, you can first download the install script and run it afterwards:
#   wget https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh
#   sh install.sh
#
# You can tweak the install behavior by setting variables when running the script. For
# example, to change the path to the Oh My Zsh repository:
#   ZSH=~/.zsh sh install.sh
#
# Respects the following environment variables:
#   ZSH     - path to the Oh My Zsh repository folder (default: $HOME/.oh-my-zsh)
#   REPO    - name of the GitHub repo to install from (default: ohmyzsh/ohmyzsh)
#   REMOTE  - full remote URL of the git repo to install (default: GitHub via HTTPS)
#   BRANCH  - branch to check out immediately after install (default: master)
#
# Other options:
#   CHSH       - 'no' means the installer will not change the default shell (default: yes)
#   RUNZSH     - 'no' means the installer will not run zsh after the install (default: yes)
#   KEEP_ZSHRC - 'yes' means the installer will not replace an existing .zshrc (default: no)
#
# You can also pass some arguments to the install script to set some these options:
#   --skip-chsh: has the same behavior as setting CHSH to 'no'
#   --unattended: sets both CHSH and RUNZSH to 'no'
#   --keep-zshrc: sets KEEP_ZSHRC to 'yes'
# For example:
#   sh install.sh --unattended
#
set -e

# Default settings
ZSH=${ZSH:-~/.oh-my-zsh}
REPO=${REPO:-ohmyzsh/ohmyzsh}
REMOTE=${REMOTE:-https://github.com/${REPO}.git}
BRANCH=${BRANCH:-master}

# Other options
CHSH=${CHSH:-yes}
RUNZSH=${RUNZSH:-yes}
KEEP_ZSHRC=${KEEP_ZSHRC:-no}


command_exists() {
    command -v "$@" >/dev/null 2>&1
}

error() {
    echo ${RED}"Error: $@"${RESET} >&2
}

setup_color() {
    # Only use colors if connected to a terminal
    if [ -t 1 ]; then
        RED=$(printf '\033[31m')
        GREEN=$(printf '\033[32m')
        YELLOW=$(printf '\033[33m')
        BLUE=$(printf '\033[34m')
        BOLD=$(printf '\033[1m')
        RESET=$(printf '\033[m')
    else
        RED=""
        GREEN=""
        YELLOW=""
        BLUE=""
        BOLD=""
        RESET=""
    fi
}

setup_ohmyzsh() {
    # Prevent the cloned repository from having insecure permissions. Failing to do
    # so causes compinit() calls to fail with "command not found: compdef" errors
    # for users with insecure umasks (e.g., "002", allowing group writability). Note
    # that this will be ignored under Cygwin by default, as Windows ACLs take
    # precedence over umasks except for filesystems mounted with option "noacl".
    umask g-w,o-w

    echo "${BLUE}Cloning Oh My Zsh...${RESET}"

    command_exists git || {
        error "git is not installed"
        exit 1
    }

    if [ "$OSTYPE" = cygwin ] && git --version | grep -q msysgit; then
        error "Windows/MSYS Git is not supported on Cygwin"
        error "Make sure the Cygwin git package is installed and is first on the \$PATH"
        exit 1
    fi

    git clone -c core.eol=lf -c core.autocrlf=false \
        -c fsck.zeroPaddedFilemode=ignore \
        -c fetch.fsck.zeroPaddedFilemode=ignore \
        -c receive.fsck.zeroPaddedFilemode=ignore \
        --depth=1 --branch "$BRANCH" "$REMOTE" "$ZSH" || {
        error "git clone of oh-my-zsh repo failed"
        exit 1
    }

    echo
}

setup_zshrc() {
    # Keep most recent old .zshrc at .zshrc.pre-oh-my-zsh, and older ones
    # with datestamp of installation that moved them aside, so we never actually
    # destroy a user's original zshrc
    echo "${BLUE}Looking for an existing zsh config...${RESET}"

    # Must use this exact name so uninstall.sh can find it
    OLD_ZSHRC=~/.zshrc.pre-oh-my-zsh
    if [ -f ~/.zshrc ] || [ -h ~/.zshrc ]; then
        # Skip this if the user doesn't want to replace an existing .zshrc
        if [ $KEEP_ZSHRC = yes ]; then
            echo "${YELLOW}Found ~/.zshrc.${RESET} ${GREEN}Keeping...${RESET}"
            return
        fi
        if [ -e "$OLD_ZSHRC" ]; then
            OLD_OLD_ZSHRC="${OLD_ZSHRC}-$(date +%Y-%m-%d_%H-%M-%S)"
            if [ -e "$OLD_OLD_ZSHRC" ]; then
                error "$OLD_OLD_ZSHRC exists. Can't back up ${OLD_ZSHRC}"
                error "re-run the installer again in a couple of seconds"
                exit 1
            fi
            mv "$OLD_ZSHRC" "${OLD_OLD_ZSHRC}"

            echo "${YELLOW}Found old ~/.zshrc.pre-oh-my-zsh." \
                "${GREEN}Backing up to ${OLD_OLD_ZSHRC}${RESET}"
        fi
        echo "${YELLOW}Found ~/.zshrc.${RESET} ${GREEN}Backing up to ${OLD_ZSHRC}${RESET}"
        mv ~/.zshrc "$OLD_ZSHRC"
    fi

    echo "${GREEN}Using the Oh My Zsh template file and adding it to ~/.zshrc.${RESET}"

    sed "/^export ZSH=/ c\\
export ZSH=\"$ZSH\"
" "$ZSH/templates/zshrc.zsh-template" > ~/.zshrc-omztemp
    mv -f ~/.zshrc-omztemp ~/.zshrc

    echo
}

setup_shell() {
    # Skip setup if the user wants or stdin is closed (not running interactively).
    if [ $CHSH = no ]; then
        return
    fi

    # If this user's login shell is already "zsh", do not attempt to switch.
    if [ "$(basename "$SHELL")" = "zsh" ]; then
        return
    fi

    # If this platform doesn't provide a "chsh" command, bail out.
    if ! command_exists chsh; then
        cat <<-EOF
            I can't change your shell automatically because this system does not have chsh.
            ${BLUE}Please manually change your default shell to zsh${RESET}
        EOF
        return
    fi

    echo "${BLUE}Time to change your default shell to zsh:${RESET}"

    # Prompt for user choice on changing the default login shell
    printf "${YELLOW}Do you want to change your default shell to zsh? [Y/n]${RESET} "
    read opt
    case $opt in
        y*|Y*|"") echo "Changing the shell..." ;;
        n*|N*) echo "Shell change skipped."; return ;;
        *) echo "Invalid choice. Shell change skipped."; return ;;
    esac

    # Check if we're running on Termux
    case "$PREFIX" in
        *com.termux*) termux=true; zsh=zsh ;;
        *) termux=false ;;
    esac

    if [ "$termux" != true ]; then
        # Test for the right location of the "shells" file
        if [ -f /etc/shells ]; then
            shells_file=/etc/shells
        elif [ -f /usr/share/defaults/etc/shells ]; then # Solus OS
            shells_file=/usr/share/defaults/etc/shells
        else
            error "could not find /etc/shells file. Change your default shell manually."
            return
        fi

        # Get the path to the right zsh binary
        # 1. Use the most preceding one based on $PATH, then check that it's in the shells file
        # 2. If that fails, get a zsh path from the shells file, then check it actually exists
        if ! zsh=$(which zsh) || ! grep -qx "$zsh" "$shells_file"; then
            if ! zsh=$(grep '^/.*/zsh$' "$shells_file" | tail -1) || [ ! -f "$zsh" ]; then
                error "no zsh binary found or not present in '$shells_file'"
                error "change your default shell manually."
                return
            fi
        fi
    fi

    # We're going to change the default shell, so back up the current one
    if [ -n "$SHELL" ]; then
        echo $SHELL > ~/.shell.pre-oh-my-zsh
    else
        grep "^$USER:" /etc/passwd | awk -F: '{print $7}' > ~/.shell.pre-oh-my-zsh
    fi

    # Actually change the default shell to zsh
    if ! chsh -s "$zsh"; then
        error "chsh command unsuccessful. Change your default shell manually."
    else
        export SHELL="$zsh"
        echo "${GREEN}Shell successfully changed to '$zsh'.${RESET}"
    fi

    echo
}

main() {
    # Run as unattended if stdin is closed
    if [ ! -t 0 ]; then
        RUNZSH=no
        CHSH=no
    fi

    # Parse arguments
    while [ $# -gt 0 ]; do
        case $1 in
            --unattended) RUNZSH=no; CHSH=no ;;
            --skip-chsh) CHSH=no ;;
            --keep-zshrc) KEEP_ZSHRC=yes ;;
        esac
        shift
    done

    setup_color

    if ! command_exists zsh; then
        echo "${YELLOW}Zsh is not installed.${RESET} Please install zsh first."
        exit 1
    fi

    if [ -d "$ZSH" ]; then
        cat <<-EOF
            ${YELLOW}You already have Oh My Zsh installed.${RESET}
            You'll need to remove '$ZSH' if you want to reinstall.
        EOF
        exit 1
    fi

    setup_ohmyzsh
    setup_zshrc
    setup_shell

    printf "$GREEN"
    cat <<-'EOF'
                 __                                     __
          ____  / /_     ____ ___  __  __   ____  _____/ /_
         / __ \/ __ \   / __ `__ \/ / / /  /_  / / ___/ __ \
        / /_/ / / / /  / / / / / / /_/ /    / /_(__  ) / / /
        \____/_/ /_/  /_/ /_/ /_/\__, /    /___/____/_/ /_/
                                /____/                       ....is now installed!


        Before you scream Oh My Zsh! please look over the ~/.zshrc file to select plugins, themes, and options.

        • Follow us on Twitter: https://twitter.com/ohmyzsh
        • Join our Discord server: https://discord.gg/ohmyzsh
        • Get stickers, shirts, coffee mugs and other swag: https://shop.planetargon.com/collections/oh-my-zsh

    EOF
    printf "$RESET"

    if [ $RUNZSH = no ]; then
        echo "${YELLOW}Run zsh to try it out.${RESET}"
        exit
    fi

    exec zsh -l
}

main "$@"
</code></pre>
<p>然后<code>cd</code>切换到桌面上，输入指令：</p>
<pre class=" language-lang-bash"><code class="language-lang-bash">sh install.sh
</code></pre>
<h4 id="3：安装PowerLevel10k主题"><a href="#3：安装PowerLevel10k主题" class="headerlink" title="3：安装PowerLevel10k主题"></a>3：安装<code>PowerLevel10k</code>主题</h4><p>依次运行以下命令</p>
<pre class=" language-lang-bash"><code class="language-lang-bash">#安装字体
sudo pacman -S nerd-fonts-complete
#安装powerlevel10k主题
sudo pacman -Sy --noconfirm zsh-theme-powerlevel10k
#配置powerlevel10k
echo 'source /usr/share/zsh-theme-powerlevel10k/powerlevel10k.zsh-theme' >>! ~/.zshrc
#使配置立即生效
source ~/.zshrc
</code></pre>
<h4 id="4-：安装自动补全插件"><a href="#4-：安装自动补全插件" class="headerlink" title="4 ：安装自动补全插件"></a>4 ：安装自动补全插件</h4><pre class=" language-lang-bash"><code class="language-lang-bash">git clone git://github.com/zsh-users/zsh-autosuggestions $ZSH_CUSTOM/plugins/zsh-autosuggestions
</code></pre>
<p>运行以下命令更改配置</p>
<pre class=" language-lang-bash"><code class="language-lang-bash">sudo nano ~/.zshrc
</code></pre>
<p>添加</p>
<pre class=" language-lang-bash"><code class="language-lang-bash">plugins=(zsh-autosuggestions git)
</code></pre>
<p>使配置立即生效</p>
<pre class=" language-lang-bash"><code class="language-lang-bash">source ~/.zshrc
</code></pre>
<h4 id="5：安装语法高亮插件"><a href="#5：安装语法高亮插件" class="headerlink" title="5：安装语法高亮插件"></a>5：安装语法高亮插件</h4><p>运行以下命令</p>
<pre class=" language-lang-bash"><code class="language-lang-bash">git clone https://github.com/zsh-users/zsh-syntax-highlighting.git $ZSH_CUSTOM/plugins/zsh-syntax-highlighting
1
</code></pre>
<p>配置插件</p>
<pre class=" language-lang-bash"><code class="language-lang-bash">echo "source $ZSH_CUSTOM/plugins/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh" >> ${ZDOTDIR:-$HOME}/.zshrc
1
</code></pre>
<p>使配置立即生效</p>
<pre class=" language-lang-bash"><code class="language-lang-bash">source ~/.zshrc
</code></pre>
<h4 id="6：sudo输入密码显示星号"><a href="#6：sudo输入密码显示星号" class="headerlink" title="6：sudo输入密码显示星号"></a>6：<code>sudo</code>输入密码显示星号</h4><pre class=" language-lang-bash"><code class="language-lang-bash">#运行命令
sudo nano /etc/sudoers
#添加以下内容
Defaults env_reset,pwfeedback
</code></pre>
<p>以上基本就是Manjoro基本配置，然后今天彻底放弃了manjoro,主要我觉得我的装完后响应真的慢，可能由于是我把系统装在了机械硬盘的原因，总之，暂时的放弃不是永久的，等到以后还会再装回来，linux必将越来越好，开源牛逼</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>manjoro</tag>
      </tags>
  </entry>
  <entry>
    <title>控制台信息转移到textedit控件上显示</title>
    <url>/WYW1314/posts/15671.html</url>
    <content><![CDATA[<h1 id="控制台信息转移到textedit控件上显示"><a href="#控制台信息转移到textedit控件上显示" class="headerlink" title="控制台信息转移到textedit控件上显示"></a>控制台信息转移到textedit控件上显示</h1><p>因为大创要做一个人脸测温的项目部署到树莓派上，所以界面是必须的，<code>pyqt5</code>比较幸运的之前我也接触过，由于需要将控制台输出的结果转移到界面上便于调试和观察，实现的具体效果为：</p>
<p><img src="https://img-blog.csdnimg.cn/20200730192343636.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>实现的具体效果大概就是这个样子，实际在程序里面写的时候还是用<code>printf()</code>语句去打印，只不过相当于把<code>printf</code>打印的地方换了一个地方输出。<br>程序里面涉及到了sys模块,sys这个模块是python中一个很重要的模块。下面是sys比较常用的函数列表：</p>
<p><code>sys.argv: 实现从程序外部向程序传递参数。</code><br><code>sys.exit([arg]): 程序中间的退出，arg=0为正常退出。</code><br><code>sys.getdefaultencoding(): 获取系统当前编码，一般默认为ascii。</code><br><code>sys.setdefaultencoding(): 设置系统默认编码，执行dir（sys）时不会看到这个方法，在解释器中执行不通过，可以先执行reload(sys)，在执行 setdefaultencoding('utf8')，此时将系统默认编码设置为utf8。（见设置系统默认编码 ）</code><br><code>sys.getfilesystemencoding(): 获取文件系统使用编码方式，Windows下返回'mbcs'，mac下返回'utf-8'.</code><br><code>sys.path: 获取指定模块搜索路径的字符串集合，可以将写好的模块放在得到的某个路径下，就可以在程序中import时正确找到。</code><br><code>sys.platform: 获取当前系统平台。</code><br><code>sys.stdin,sys.stdout,sys.stderr: stdin , stdout , 以及stderr 变量包含与标准I/O 流对应的流对象. 如果需要更好地控制输出,而print 不能满足你的要求, 它们就是你所需要的. 你也可以替换它们, 这时候你就可以重定向输出和输入到其它设备( device ), 或者以非标准的方式处理它们</code></p>
<p>思路的话参考的网上其他大佬的程序，</p>
<h2 id="1：定义一个类和定义一个信号和槽"><a href="#1：定义一个类和定义一个信号和槽" class="headerlink" title="1：定义一个类和定义一个信号和槽"></a>1：定义一个类和定义一个信号和槽</h2><p>具体思路是首先定义一个类，程序具体为：</p>
<pre class=" language-lang-python"><code class="language-lang-python">class EmittingStr(QtCore.QObject):
        textWritten = QtCore.pyqtSignal(str)  # 定义一个str的信号
        def write(self, text):
            self.textWritten.emit(str(text))
            loop = QEventLoop()
            QTimer.singleShot(1000, loop.quit)
            loop.exec_()
</code></pre>
<p><code>QtCore.pyqtSignal</code>的作用是创建一个<code>str</code>的信号，之后就对这个创建的信号进行操作，这部分又涉及到了<code>Pyqt5</code>中的信号与槽的机制的知识，<u><del>莫名觉得又挖了一个坑进去</del></u>。下面可能废话比较多，主要讲讲信号与槽的机制原理：</p>
<p>下面是信号与槽机制的示意图：</p>
<p><img src="https://img-blog.csdnimg.cn/20200730205314124.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>当事件或者状态发生改变时，就会发出信号，同时的话，信号也会触发所有与这个事件(信号)相关的函数(槽)，信号与槽的话可以多对多的关系，一个信号可以连接多个槽，一个槽也可以监听多个信号。</p>
<p>由于<code>Pyqt</code>的内置信号是自动定义的。使用<code>QtCore.pyqtSignal()</code>函数可以为<code>Qobject</code>创建一个信号，使用这个函数可以自定义信号的属性，使用这个函数创建信号时候，信号可以传递多个参数，并指定信号传递参数的类型，参数类型必须是标准的Python数据类型(字符串，日期，布尔类型，数字，列表，元组和字典)。信号与槽的运用我打算单独再写出一篇。又挖了一个大坑，啊哈哈哈哈，o(<em>￣︶￣</em>)o。</p>
<p>程序下面一句自定义了一个write函数，第一句emit函数的作用就是可以发射一个信号，这个步骤也是信号与槽的必须的一个步骤。</p>
<p><code>QEventLoop</code>这个函数的作用就是创建一个局部的循环。</p>
<ul>
<li><p>创建事件循环</p>
</li>
<li><p>启动定时器，让其<code>100ms</code>后触发事件循环的quit()槽</p>
</li>
<li><p>启动事件循环</p>
</li>
</ul>
<h2 id="2：写一个关于操控textedit控件的函数："><a href="#2：写一个关于操控textedit控件的函数：" class="headerlink" title="2：写一个关于操控textedit控件的函数："></a>2：写一个关于操控<code>textedit</code>控件的函数：</h2><pre class=" language-lang-python"><code class="language-lang-python">   def outputWritten(self, text):
        # self.textEdit.clear()
        cursor = self.textEdit.textCursor()
        cursor.movePosition(QtGui.QTextCursor.End)
        cursor.insertText(text)
        self.textEdit.setTextCursor(cursor)
        self.textEdit.ensureCursorVisible()
</code></pre>
<p>这一段程序主要是对<code>texteidt</code>这个控件进行文本操作。这里涉及到了光标操作(我自己给自己又挖了一个坑，一会这篇写完专门写一篇来介绍这个光标位置的使用，这一段程序可以理解为对文本的操作。</p>
<p>3：最后在你的类里面添加这俩句程序即可：</p>
<pre class=" language-lang-python"><code class="language-lang-python">        sys.stdout = EmittingStr(textWritten=self.outputWritten)
        sys.stderr = EmittingStr(textWritten=self.outputWritten)
</code></pre>
<p><code>sys.stdout</code>和<code>sys.stderr</code>这俩个就要划重点了。</p>
<p><code>sys.stdout</code> 与 print的作用基本类似，可以看作print就是<code>sys.stdout</code>的进一步封装，们在 Python 中打印对象调用 print obj 时候，事实上是调用了 <code>sys.stdout.write(obj+'\n')</code>，print 将你需要的内容打印到了控制台，然后追加了一个换行符<br>print 会调用 <code>sys.stdout</code> 的 write 方法以下两行在事实上等价：</p>
<pre class=" language-lang-python"><code class="language-lang-python">sys.stdout.write('hello'+'\n') 
print 'hello'
</code></pre>
<p>咱们的目的是把控制台信息重定向到<code>Textedit</code>上，下面有一个网上的例子很好理解，就是把控制台的信息同时定向到文件和控制台输出。</p>
<p>具体代码：</p>
<pre class=" language-lang-python"><code class="language-lang-python">f_handler=open('out.log', 'w') 
sys.stdout=f_handler 
print("Hello World")
</code></pre>
<p>这个就是把控制台输出的信息存储到文件当中，也就是<code>out.log</code>这个文件中。看完这个例子是不是有了想法，咱们把是不是只要把控制<code>Textedit</code>的赋值给<code>sys.stdout</code>,这样不就可以了。</p>
<p>是不是非常的amazing！！！。<code>sys.stderr</code>这个东东我百度说的是用<code>sys.stderr</code> 目的就是返回错误信息,但是我在界面里面的程序尝试写了一个1%0的bug，发现<code>Textedit</code>里面并没有显示相应的错误。莫非<code>pyqt5</code>界面和这个有冲突。</p>
<p>抱着试一试的态度，我单独写了一个程序实验，发现单独用是好使的：</p>
<pre class=" language-lang-python"><code class="language-lang-python">f_handler=open('out.log', 'w')
sys.stderr=f_handler
a = 1%0
print(a)
</code></pre>
<p>这很明显有错误，对吧。然后我打开生成的<code>out.log</code>,发现错误内容被记录了进去，就说明该程序没有错误。</p>
<p><img src="https://img-blog.csdnimg.cn/20200731143159910.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>至于为啥那个界面不显示，暂且没找到原因。以上就大概完成这个功能。另外俩个坑很快就会填上.</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>qt界面</category>
      </categories>
      <tags>
        <tag>qtdesigner</tag>
      </tags>
  </entry>
  <entry>
    <title>Pyinstaller将Py文件打包为EXE可执行文件</title>
    <url>/WYW1314/posts/42312.html</url>
    <content><![CDATA[<h1 id="Pyinstaller将Py文件打包为EXE可执行文件"><a href="#Pyinstaller将Py文件打包为EXE可执行文件" class="headerlink" title="Pyinstaller将Py文件打包为EXE可执行文件"></a>Pyinstaller将Py文件打包为EXE可执行文件</h1><h2 id="1-python作为一门解释型脚本语言，它有三种发布方式"><a href="#1-python作为一门解释型脚本语言，它有三种发布方式" class="headerlink" title="1:python作为一门解释型脚本语言，它有三种发布方式"></a>1:python作为一门解释型脚本语言，它有三种发布方式</h2><ol>
<li>.py文件 : 源码文件，运行需要使用者安装Python环境并且安装依赖的各种库</li>
<li>.pyc文件：pyc文件是Python解释器可以识别的二进制码，可跨平台的，需要使用者安装相应版本的Python和依赖库。</li>
<li>可执行文件：不需要安装python环境和依赖库,可针对不同平台需要打包不同的可执行文件（Windows,Linux,Mac,…）</li>
</ol>
<h2 id="2-pyinstaller原理详解"><a href="#2-pyinstaller原理详解" class="headerlink" title="2:pyinstaller原理详解"></a>2:pyinstaller原理详解</h2><ol>
<li>PyInstaller工具可以把python解析器和脚本打包成一个可执行的文件，并不是编译成真正的机器码，打包成一个可执行文件后运行效率可能会降低，好处就是在使用者的机器上可以不用安装python和你的脚本依赖的库。 2. 利用PyInstaller对指定的的脚本打包时，会先分析脚本所依赖的其他脚本，然后根据导包路径去查找，把所有相关的脚本收集起来，包括Python解析器，然后根据你的命令参数可分别生成文件夹，或者打包成一个可执行文件。 3. 无论是生成的文件夹里的可执行文件或者只打包成一个可执行文件都可以直接运行，前者需要把整个文件夹都给别人。</li>
</ol>
<h2 id="3：pyinstaller库的安装："><a href="#3：pyinstaller库的安装：" class="headerlink" title="3：pyinstaller库的安装："></a>3：pyinstaller库的安装：</h2><h3 id="1-PyInstaller在windows下的安装"><a href="#1-PyInstaller在windows下的安装" class="headerlink" title="1. PyInstaller在windows下的安装"></a>1. PyInstaller在windows下的安装</h3><p>我安装的pip命令比较奇葩，明明电脑上只有一个Python3.7，但是单独用pip命令安装的时候安装的包自动安装到anaconda下的目录下，贼头大，暂且没找到什么原因，一般直接用pip命令安装就可以了，</p>
<p><code>pip install PyInstaller</code> ,window上还需要pywin32的支持。一般以上这条指令后，电脑会自动安装pywin32依赖库。</p>
<h2 id="4：pyinstaller命令参数详解"><a href="#4：pyinstaller命令参数详解" class="headerlink" title="4：pyinstaller命令参数详解"></a>4：pyinstaller命令参数详解</h2><p><img src="https://img-blog.csdnimg.cn/20200727094426266.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="img"></p>
<p>与生成结果有关参数：</p>
<p><img src="https://img-blog.csdnimg.cn/20200727094716542.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="img"></p>
<p>window下特有的参数：</p>
<p><img src="https://img-blog.csdnimg.cn/20200727095155971.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="img"></p>
<p>基本语法指令：</p>
<p><code>PyInstaller -F -w -i  xxx.ico dev.py --hidden-import=pandas._libs.tslibs.timedeltas</code></p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>qt界面</category>
      </categories>
      <tags>
        <tag>pyinstaller</tag>
      </tags>
  </entry>
  <entry>
    <title>解决Pyqt5每次修改Ui界面对应的Py文件都要改变</title>
    <url>/WYW1314/posts/11865.html</url>
    <content><![CDATA[<h1 id="鸽王回来了"><a href="#鸽王回来了" class="headerlink" title="鸽王回来了"></a>鸽王回来了</h1><p>自己的博客也不知道鸽了多久，只记得从六月份打完数学建模后就接着期末考试。期末考试完然后就去接着学车，考驾照，这个月终于要把驾照的事情给忙完了，哈哈哈，开心:happy:，可以接着在家安心宅着写程序了。</p>
<p>一直以来使用<code>pyqt5</code>时候会遇到ui文件反复修改的问题，然后<code>ui</code>文件每次修改的时候导出的Python的程序都会跟着改变，然而以前自己太傻了，每次都重复做着相同的没有意义的事情，把覆盖掉的代码再重新一行一行接着码上，那个过程贼酸爽。:cry:,知道某天突然脑袋灵光了一阵，</p>
<p>想到了既然每次写程序那个<code>Ui</code>转换出来的<code>Py</code>文件都是作为父类来继承，那么咱们可以创建一个新的Py文件，来继承父类的各项东西以及属性，这个没毛病吧。下面举一个例子：</p>
<pre class=" language-lang-python"><code class="language-lang-python">from PyQt5 import QtCore, QtGui, QtWidgets


class Ui_facecollectMainWindow(object):
    def setupUi(self, facecollectMainWindow):
        facecollectMainWindow.setObjectName("facecollectMainWindow")
        facecollectMainWindow.resize(1142, 651)
        self.centralwidget = QtWidgets.QWidget(facecollectMainWindow)
        self.centralwidget.setObjectName("centralwidget")
        self.face_showlabel = QtWidgets.QLabel(self.centralwidget)
        self.face_showlabel.setGeometry(QtCore.QRect(290, 80, 531, 401))
        self.face_showlabel.setText("")
        self.face_showlabel.setAlignment(QtCore.Qt.AlignCenter)
        self.face_showlabel.setObjectName("face_showlabel")
        self.stu_numberlineEdit = QtWidgets.QLineEdit(self.centralwidget)
        self.stu_numberlineEdit.setGeometry(QtCore.QRect(980, 130, 141, 21))
        self.stu_numberlineEdit.setObjectName("stu_numberlineEdit")
        self.label = QtWidgets.QLabel(self.centralwidget)
        self.label.setGeometry(QtCore.QRect(930, 130, 41, 16))
        self.label.setObjectName("label")
        self.stu_namelabel_2 = QtWidgets.QLabel(self.centralwidget)
        self.stu_namelabel_2.setGeometry(QtCore.QRect(930, 180, 31, 16))
        self.stu_namelabel_2.setObjectName("stu_namelabel_2")
        self.lineEdit = QtWidgets.QLineEdit(self.centralwidget)
        self.lineEdit.setGeometry(QtCore.QRect(980, 180, 141, 21))
        self.lineEdit.setObjectName("lineEdit")
        self.storebuttonBox = QtWidgets.QDialogButtonBox(self.centralwidget)
        self.storebuttonBox.setGeometry(QtCore.QRect(930, 230, 193, 28))
        self.storebuttonBox.setStandardButtons(QtWidgets.QDialogButtonBox.Cancel|QtWidgets.QDialogButtonBox.Ok)
        self.storebuttonBox.setObjectName("storebuttonBox")
        self.layoutWidget = QtWidgets.QWidget(self.centralwidget)
        self.layoutWidget.setGeometry(QtCore.QRect(10, 560, 316, 30))
        self.layoutWidget.setObjectName("layoutWidget")
        self.horizontalLayout = QtWidgets.QHBoxLayout(self.layoutWidget)
        self.horizontalLayout.setContentsMargins(0, 0, 0, 0)
        self.horizontalLayout.setObjectName("horizontalLayout")
        self.label_3 = QtWidgets.QLabel(self.layoutWidget)
        self.label_3.setObjectName("label_3")
        self.horizontalLayout.addWidget(self.label_3)
        self.set_photospinBox = QtWidgets.QSpinBox(self.layoutWidget)
        self.set_photospinBox.setObjectName("set_photospinBox")
        self.horizontalLayout.addWidget(self.set_photospinBox)
        self.start_recordpushButton = QtWidgets.QPushButton(self.layoutWidget)
        self.start_recordpushButton.setObjectName("start_recordpushButton")
        self.horizontalLayout.addWidget(self.start_recordpushButton)
        self.faceNumber = QtWidgets.QLCDNumber(self.layoutWidget)
        self.faceNumber.setObjectName("faceNumber")
        self.horizontalLayout.addWidget(self.faceNumber)
        self.label_2 = QtWidgets.QLabel(self.centralwidget)
        self.label_2.setGeometry(QtCore.QRect(440, 10, 211, 41))
        font = QtGui.QFont()
        font.setPointSize(15)
        font.setBold(True)
        font.setItalic(False)
        font.setWeight(75)
        self.label_2.setFont(font)
        self.label_2.setObjectName("label_2")
        self.layoutWidget1 = QtWidgets.QWidget(self.centralwidget)
        self.layoutWidget1.setGeometry(QtCore.QRect(10, 10, 312, 32))
        self.layoutWidget1.setObjectName("layoutWidget1")
        self.horizontalLayout_3 = QtWidgets.QHBoxLayout(self.layoutWidget1)
        self.horizontalLayout_3.setContentsMargins(0, 0, 0, 0)
        self.horizontalLayout_3.setObjectName("horizontalLayout_3")
        self.horizontalLayout_2 = QtWidgets.QHBoxLayout()
        self.horizontalLayout_2.setObjectName("horizontalLayout_2")
        self.opencamerapushButton_2 = QtWidgets.QPushButton(self.layoutWidget1)
        self.opencamerapushButton_2.setObjectName("opencamerapushButton_2")
        self.horizontalLayout_2.addWidget(self.opencamerapushButton_2)
        self.extern_cameracheckBox = QtWidgets.QCheckBox(self.layoutWidget1)
        self.extern_cameracheckBox.setObjectName("extern_cameracheckBox")
        self.horizontalLayout_2.addWidget(self.extern_cameracheckBox)
        self.horizontalLayout_3.addLayout(self.horizontalLayout_2)
        self.openface_pushButton = QtWidgets.QPushButton(self.layoutWidget1)
        self.openface_pushButton.setObjectName("openface_pushButton")
        self.horizontalLayout_3.addWidget(self.openface_pushButton)
        self.layoutWidget2 = QtWidgets.QWidget(self.centralwidget)
        self.layoutWidget2.setGeometry(QtCore.QRect(840, 10, 189, 26))
        self.layoutWidget2.setObjectName("layoutWidget2")
        self.horizontalLayout_4 = QtWidgets.QHBoxLayout(self.layoutWidget2)
        self.horizontalLayout_4.setContentsMargins(0, 0, 0, 0)
        self.horizontalLayout_4.setObjectName("horizontalLayout_4")
        self.timeEdit = QtWidgets.QTimeEdit(self.layoutWidget2)
        self.timeEdit.setObjectName("timeEdit")
        self.horizontalLayout_4.addWidget(self.timeEdit)
        self.dateEdit = QtWidgets.QDateEdit(self.layoutWidget2)
        self.dateEdit.setObjectName("dateEdit")
        self.horizontalLayout_4.addWidget(self.dateEdit)
        facecollectMainWindow.setCentralWidget(self.centralwidget)
        self.menubar = QtWidgets.QMenuBar(facecollectMainWindow)
        self.menubar.setGeometry(QtCore.QRect(0, 0, 1142, 29))
        self.menubar.setObjectName("menubar")
        facecollectMainWindow.setMenuBar(self.menubar)
        self.statusbar = QtWidgets.QStatusBar(facecollectMainWindow)
        self.statusbar.setObjectName("statusbar")
        facecollectMainWindow.setStatusBar(self.statusbar)

        self.retranslateUi(facecollectMainWindow)
        QtCore.QMetaObject.connectSlotsByName(facecollectMainWindow)

    def retranslateUi(self, facecollectMainWindow):
        _translate = QtCore.QCoreApplication.translate
        facecollectMainWindow.setWindowTitle(_translate("facecollectMainWindow", "MainWindow"))
        self.label.setText(_translate("facecollectMainWindow", "学号"))
        self.stu_namelabel_2.setText(_translate("facecollectMainWindow", "姓名"))
        self.label_3.setText(_translate("facecollectMainWindow", "设置采集帧数"))
        self.start_recordpushButton.setText(_translate("facecollectMainWindow", "开始采集"))
        self.label_2.setText(_translate("facecollectMainWindow", "人脸识别测温系统"))
        self.opencamerapushButton_2.setText(_translate("facecollectMainWindow", "打开摄像头"))
        self.extern_cameracheckBox.setText(_translate("facecollectMainWindow", "外部摄像头"))
        self.openface_pushButton.setText(_translate("facecollectMainWindow", "打开人脸识别"))
</code></pre>
<p>假如上面就是一个ui界面经过Pyuic工具转换后的程序。这个程序就相当于我们的父类程序，我们不要在该程序写自己的方法进去，不然后期ui界面的优化修改会被覆盖掉，（但是如果你是纯手码写界面的话，那就完全不需要这样子）</p>
<p>我们可以新建一个程序上去，然后写一个类来继承父类，程序可以是这个样子：</p>
<pre class=" language-lang-python"><code class="language-lang-python">class myfacewindow(QtWidgets.QMainWindow, Ui_facecollectMainWindow):
    def __init__(self):
        super(myfacewindow, self).__init__()
        self.setupUi(self)
        #定义一个存储名字以及学号的字典
        self.studentdata = {'stu_name':'','stu_id':''}
        #输入框中提示内容
        self.stu_numberlineEdit.setPlaceholderText('输入你的学号')
        self.lineEdit.setPlaceholderText('输入姓名')
        #照片存储路径
        self.path_photos_from_camera = "data/data_faces_from_camera/"
        #opencv以及dlib
        self.cap = cv2.VideoCapture()
        # Dlib 正向人脸检测器 / frontal face detector
        self.detector = dlib.get_frontal_face_detector()
        # 定时器
        self.timer = QTimer(self)
        self.timer.timeout.connect(self.updateFrame)
        # 连接相应的槽函数
        self.isExternalCameraUsed = False#外部摄像头控制选择变量
        self.isFaceDetectEnabled = False
        self.openface_pushButton.setEnabled(False)#未打开摄像头时候人脸识别的按钮是不能被打开的
        self.openface_pushButton.toggled.connect(self.enableFaceDetect)
        self.openface_pushButton.setCheckable(True)
        self.opencamerapushButton_2.toggled.connect(self.startWebcam)#打开摄像头
        self.opencamerapushButton_2.setCheckable(True)
        self.face_showlabel.setText('<font color=red>摄像头未开启</font>')#未打开摄像头时候显示文字摄像头未开启
        self.setFixedSize(self.width(),self.height())#固定窗口尺寸
        self.extern_cameracheckBox.stateChanged.connect(self.useExternalCamera)#选择内置摄像头还是外置摄像头
        #存储数据
        self.storebuttonBox.accepted.connect(self.store_message)
        self.storebuttonBox.rejected.connect(self.clear_message)
    #将输入框中的信息存储起来
    def store_message(self):
        self.studentdata['stu_name'] = self.lineEdit.text()#获得输入框姓名
        self.studentdata['stu_id'] = self.stu_numberlineEdit.text()#学生的学号
        print(self.studentdata)
    #将输入框中的内容全部清空
    def clear_message(self):
        self.lineEdit.setText('')
        self.stu_numberlineEdit.setText('')
    #将拍摄的照片存储起来和设置好多少帧
    # def record_photo(self,fps):
    # 开启/关闭人脸检测
    def enableFaceDetect(self, status):
        if self.cap.isOpened():
            if status:
                self.openface_pushButton.setText('关闭人脸识别')
                self.isFaceDetectEnabled = True
            else:
                self.openface_pushButton.setText('打开人脸识别')
                self.isFaceDetectEnabled = False
        # 检测人脸
    def detectFace(self, frame):
        # 人脸 / Faces
        faces = self.detector(frame, 0)
        font = cv2.FONT_ITALIC
        # 检测到人脸 / Face detected
        if len(faces) != 0:
            # 矩形框 / Show the rectangle box of face
            for k, d in enumerate(faces):
                # 计算矩形大小
                # Compute the width and height of the box
                # (x,y), (宽度width, 高度height)
                pos_start = tuple([d.left(), d.top()])
                pos_end = tuple([d.right(), d.bottom()])
                # 计算矩形框大小 / compute the size of rectangle box
                height = (d.bottom() - d.top())
                width = (d.right() - d.left())
                hh = int(height / 2)
                ww = int(width / 2)
                # 设置颜色 / the color of rectangle of faces detected
                color_rectangle = (255, 255, 255)
                # 判断人脸矩形框是否超出 480x640
                if (d.right() + ww) > 640 or (d.bottom() + hh > 480) or (d.left() - ww < 0) or (d.top() - hh < 0):
                    cv2.putText(frame, "OUT OF RANGE", (20, 300), font, 0.8, (0, 0, 255), 1, cv2.LINE_AA)
                    color_rectangle = (0, 0, 255)
                    print("请调整位置 / Please adjust your position")
                else:
                    color_rectangle = (255, 255, 255)
                    save_flag = 1
                cv2.rectangle(frame,
                              tuple([d.left() - ww, d.top() - hh]),
                              tuple([d.right() + ww, d.bottom() + hh]),
                              color_rectangle, 2)
        return frame
    # 定时器，实时更新画面
    def updateFrame(self):
        ret, frame = self.cap.read()
        if ret:
            self.displayImage(frame)
            if self.isFaceDetectEnabled:
                detected_frame = self.detectFace(frame)
                self.displayImage(detected_frame)
            else:
                self.displayImage(frame)
    # 是否使用外接摄像头
    def useExternalCamera(self):
        if self.extern_cameracheckBox.isChecked():
            self.isExternalCameraUsed = True
        else:
            self.isExternalCameraUsed = False
    #打开摄像头
    def startWebcam(self, status):
        if status:
            if self.isExternalCameraUsed:
                camID = 1
            else:
                camID = 0
            self.cap.open(camID)
            self.cap.set(cv2.CAP_PROP_FRAME_WIDTH, 640)
            self.cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 480)
            ret, frame = self.cap.read()
            if not ret:
                self.cap.release()
                self.opencamerapushButton_2.setChecked(False)
            else:
                self.opencamerapushButton_2.setText('关闭摄像头')
                self.openface_pushButton.setEnabled(True)
                self.timer.start(5)
        else:
            if self.cap.isOpened():
                if self.timer.isActive():
                    self.timer.stop()
                self.cap.release()
                self.face_showlabel.clear()
                self.face_showlabel.setText('<font color=red>摄像头未开启</font>')
                self.openface_pushButton.setEnabled(False)
                self.opencamerapushButton_2.setText('打开摄像头')

        # 显示图像
    def displayImage(self, img):
        # BGR -> RGB
        img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
        # default：The image is stored using 8-bit indexes into a colormap， for example：a gray image
        qformat = QImage.Format_Indexed8
        if len(img.shape) == 3:  # rows[0], cols[1], channels[2]
            if img.shape[2] == 4:
                # The image is stored using a 32-bit byte-ordered RGBA format (8-8-8-8)
                # A: alpha channel，不透明度参数。如果一个像素的alpha通道数值为0%，那它就是完全透明的
                qformat = QImage.Format_RGBA8888
            else:
                qformat = QImage.Format_RGB888

        # img.shape[1]：图像宽度width，img.shape[0]：图像高度height，img.shape[2]：图像通道数
        # QImage.__init__ (self, bytes data, int width, int height, int bytesPerLine, Format format)
        # 从内存缓冲流获取img数据构造QImage类
        # img.strides[0]：每行的字节数（width*3）,rgb为3，rgba为4
        # strides[0]为最外层(即一个二维数组所占的字节长度)，strides[1]为次外层（即一维数组所占字节长度），strides[2]为最内层（即一个元素所占字节长度）
        # 从里往外看，strides[2]为1个字节长度（uint8），strides[1]为3*1个字节长度（3即rgb 3个通道）
        # strides[0]为width*3个字节长度，width代表一行有几个像素

        outImage = QImage(img, img.shape[1], img.shape[0], img.strides[0], qformat)
        self.face_showlabel.setPixmap(QPixmap.fromImage(outImage))
        self.face_showlabel.setScaledContents(True)
if __name__ == '__main__':
    app = QApplication(sys.argv)
    window = myfacewindow()
    window.show()
    sys.exit(app.exec_())
</code></pre>
<p>我们将自己要写的功能都可以在新建的这个Python文件去加，这样即使界面那个Py文件有所修改的话这里的话也不用担心被覆盖掉或者怎么样。:+1:,是不是非常的amazing!!!!!!.</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>qt界面</category>
      </categories>
      <tags>
        <tag>qtdesigner</tag>
      </tags>
  </entry>
  <entry>
    <title>梯度下降法理解</title>
    <url>/WYW1314/posts/57153.html</url>
    <content><![CDATA[<h1 id="梯度下降法理解"><a href="#梯度下降法理解" class="headerlink" title="梯度下降法理解"></a>梯度下降法理解</h1><p><strong><em>梯度就是梯度啊，是一个矢量，大小为最大的方向导数，方向为最大方向导数的方向。但在单变量的实值函数的情况，梯度只是导数梯度</em></strong>。在只有一个维度的情况下简单可以理解为就是咱们学习的导数。导数是啥想必大家都很清楚了。</p>
<p>那么 <strong>梯度下降就是用来求某个函数最小值时自变量对应取值。</strong>这个函数名字叫做损失函数（cost/loss function），机器学习涉及到了最基础的概念，直白点就是误差函数。一个算法不同参数会产生不同拟合曲线，也意味着有不同的误差。<strong>损失函数就是一个自变量为算法的参数，函数值为误差值的函数。梯度下降就是找让误差值最小时候算法取的参数。</strong></p>
<p>深度学习的话视频推荐吴恩达的视频，他的视频非常透彻。网易云课堂上能给有搜到相关的视频。机器学习中涉及到一个损失函数的概念，<strong>有一类算法</strong>就是产生一条曲线来拟合现有的数据，这样子就可以实现预测未来的数据，这个专业术语叫做回归（见到回归就替换成拟合就好了~^~）。<strong>还有另外一种类</strong>似也是产生一条曲线，但是这个曲线时用来将点分隔成两块，实现分类，在这个曲线一侧为一类另外一侧算一类。<strong>但是我怎么知道这个算法产生的拟合曲线效果好不好呢？</strong>这个判断效果的名词叫做误差，预测值减去真实值最后取绝对值，于是乎就产生了损失函数这一概念。那么如何找到这个损失函数值最小的点呢，咱们高中的时候求最大或者最小值时候是不是通过求导来求极值点，那么求损失函数的最小值也是这样。可以通过求导的方式去进行。前面的时候也已经说到过。梯度就是求导数。梯度下降的话就是求极小值的点。<br>那么用梯度下降的方法的去求最小值的时候，我们可以这样去理解，假如我们夜晚现在要下山，现在手里要拿着手电筒，我们如果照亮的一段路是下坡，我们就会接着往坡下走，这样一定最后能够到达山脚。那么梯度下降的原理也类似上面这样。<br>梯度下降含义（具体操作）是：改变x的值使得导数的绝对值变小，当导数小于0时候（情况1），我们要让目前x值大一点点，再看它导数值。当导数大于0时候（情况2），我们要让目前x值减小一点点，再看它导数值。当导数接近0时候，我们就得到想要的自变量x了。也就是说找到这个算法最佳参数，使得拟合曲线与真实值误差最小。害。自己太菜了。这次参加的比赛完全在划水，也没帮上啥忙。</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>梯度下降</tag>
      </tags>
  </entry>
  <entry>
    <title>心情Note</title>
    <url>/WYW1314/posts/63933.html</url>
    <content><![CDATA[<h1 id="心情Note："><a href="#心情Note：" class="headerlink" title="心情Note："></a>心情Note：</h1><p>疫情这段期间发生了太多事情，这半年。先是四月初被女朋友分手，好不容易从里面走出来，接下来因为项目的事情得罪了俩个老师。接下来自己刚开始从大一开始呆的社团感觉变味了，想着想要离开社团。终于在前天晚上开会后坚定了这个想法。自己也算是真正考虑清楚了把。自己退出后我也挺庆幸，能够看清楚身边人到底都是真心处的朋友和俩肋插刀的朋友。自己以为平时在社团无论谁叫我帮忙我都会去帮。以为走后会有人去给我唠唠，没想到还是你们俩个，健哥你和老狗你。失恋的时候也是你们。害，说实话自己昨天晚上做了老多梦，里面都是以前的回忆吧，大一刚来的时候和杨秀奇还不认识，那时候自我介绍我就说我去了电协嘛，然后老狗你知道了，之后就认识了老狗你，那时候和你一人买了一个二手车，天天大一有晚自习的时候自己一有时间就呆到晚自习上课前十几分钟，不吃饭疯狂骑车子从工程楼到电信楼，每次电信楼那个大上坡真的累死人。害，还有次然后焊一个音响白，我焊了六七次都还不好使。然后健哥一遍成。我当时还被嘲笑，哈哈。接下来就是寒假了白，我和老狗你就商量校赛的事情，咱们就商量了做一个物联网大棚，然后我假期学习32，你学电源的东西，然后很快开学了。校赛第一周，我们基本就快整完了。然后被要求换题，审核的第一周的时候那天晚上，真的算是非常自闭的一个晚上把。当时回到寝室后，寝室还停电了，其实自己大学够幸运了，能够遇到一群好的室友和你们俩个好朋友。之后换题换成风洞，然后第三周的时候做不完了，咱们就打算包宿，但是那天也发生了不愉快的事情吧，不过也都过去了。校赛后，假期备战电赛的时候压力也很大，不过那时候老狗你和健哥你的座位你们在我后面，天天没事就唠嗑，哈哈哈哈。想想就挺开心的，那段时间。打完电赛那个暑假后，我们又一块参加了物理实验竞赛，虽然物理实验过程中有点小插曲，但是一块录视频，去徐老师那边填表那段日子真的很开心呢，徐老师这个老师也真的很好。虽然最后的结果不是特别好，但是很开心能经历这次比赛，之后就大二了，要竞选部门，我和老狗你都想去培训部把自己会的教下去，现在我的退出可能也违背自己的初心了吧，健哥说的很对，其实电协传承的不应该只有知识，还有电协人的精神，但是我退出的原因是我自己感觉不到这种感觉了，感觉就是为了培训而培训。没有了所谓的精神。成了所谓的面子活。安排时候从来就想着和去年怎么怎么样，害，过去就过去了吧。不提了。我知道昨晚你俩给我唠是因为担心我的以后会受到影响，嘿嘿，你俩不用担心，我已经有了自己的想法，会一步一步继续走下去的，感觉写着写着像毕业分离一样，整的很感伤。哈哈哈，以后我不在社团也会天天见面啊。哈哈哈。加油吧。</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>生活随笔</category>
      </categories>
      <tags>
        <tag>心情</tag>
      </tags>
  </entry>
  <entry>
    <title>Python导入qtdesigner和pyuic</title>
    <url>/WYW1314/posts/37573.html</url>
    <content><![CDATA[<h1 id="Python导入qtdesigner和pyuic"><a href="#Python导入qtdesigner和pyuic" class="headerlink" title="Python导入qtdesigner和pyuic"></a>Python导入qtdesigner和pyuic</h1><h2 id="1：cmd安装qtsdesigner和pyuic："><a href="#1：cmd安装qtsdesigner和pyuic：" class="headerlink" title="1：cmd安装qtsdesigner和pyuic："></a>1：cmd安装qtsdesigner和pyuic：</h2><pre class=" language-lang-python"><code class="language-lang-python">pip install pyqt5
pip install pyqt5_tools
</code></pre>
<h2 id="2-通过Pycharm导入pyqt5："><a href="#2-通过Pycharm导入pyqt5：" class="headerlink" title="2:通过Pycharm导入pyqt5："></a>2:通过Pycharm导入pyqt5：</h2><p>我的Pycharm是汉化过的，所以找到设置-外部工具：点击加号添加：</p>
<p><img src="https://img-blog.csdnimg.cn/20200420224710879.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200420221334912"></p>
<p>会出来以下的对话框：</p>
<p><img src="https://img-blog.csdnimg.cn/20200420224609811.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt="image-20200420221742615"></p>
<p>name自定义，最好能是你自己定义的跟这个外部工具有关的。program为designer.exe所在的路径,通常情况为：你安装的python目录下的：\Lib\site-packages\pyqt5_tools\Qt\bin\designer.exe。剩下不用管即可，</p>
<p>将保存的ui文件转换为py文件可以添加另外一个脚本，也就是pyuic.py.</p>
<p>Name：和上面一样，随便填。这里填PyUIC</p>
<p>Program：填自己的<strong>python路径</strong>，不知道的在(cmd)终端输入命令where python 查询。</p>
<p>Parameters：首先放入自己的pyuic.py的路径。以后文件路径不知道的在我的电脑搜，我就不再重复了。<strong>空一个格</strong>后直接粘贴我的这个就行。<strong>$FileName$ -o $FileNameWithoutExtension$.py</strong> 这一行命令的意思应该是用pyuic.py 将文件名.ui文件转换成文件名.py文件。</p>
<p>Working directory：$ProjectFileDir$。</p>
<p>完成后转换可能会报错误，这里只需要修改俩个地方即可：</p>
<p>1）主义在pyuic.py文件路径和$FileName$ -o $FileNameWithoutExtension$.py之间<strong>加空格</strong>。不然会报错。</p>
<p>2）from .driver import Driver 报错，将此行改成 <strong>from PyQt5.uic.driver import Driver</strong></p>
<p>同理将下面那个报错也修改成   <strong>from PyQt5.uic.exceptions import NoSuchClassError, NoSuchWidgetError</strong></p>
<p>以上就完成了。</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>qt界面</category>
      </categories>
      <tags>
        <tag>qtdesigner</tag>
      </tags>
  </entry>
  <entry>
    <title>钢铁表面缺陷检测</title>
    <url>/WYW1314/posts/32968.html</url>
    <content><![CDATA[<h1 id="钢铁表面缺陷检测"><a href="#钢铁表面缺陷检测" class="headerlink" title="钢铁表面缺陷检测"></a>钢铁表面缺陷检测</h1><p><u><del><strong>勿喷，感觉自己写的老菜了，都是参考别人写的论文搞的。</strong></del></u></p>
<p>4月末的某一天，老师打电话问我对一个比赛有没有兴趣。关于工业质量钢板质量检测的，当时也就答应了，其实自己有好多事情要去做。但是还是答应了，于是就去做。其实自己也没有啥思路，自己去找了找网上相关的论文。其中有一篇挺好的。照着里面的思路慢慢去做。发现识别出来的效果还行。</p>
<h2 id="主要方法："><a href="#主要方法：" class="headerlink" title="主要方法："></a>主要方法：</h2><h3 id="1对图片进行top-hat变换："><a href="#1对图片进行top-hat变换：" class="headerlink" title="1对图片进行top-hat变换："></a>1对图片进行top-hat变换：</h3><p>Top-hat 去光照不均图像增强方法 数学形态学是在集合的层次上来描述和分析图像，着重研究图像的几何结构。<br>腐蚀和膨胀是形态学的两种最基本的运算，从数学角度来说，就是将图像与结构元素进行卷积。常见的数学形态学有二值形态学和灰度形态学。灰度形态学就是直接对灰度图像进行形态操作，二值形态学是对二值化的图像进行形态操作。形<br>态学运算在图像处理领域常被用来进行图像分割，特征提取，有的文献也介绍了用形态学运算对图像进行滤波处理，而且效果很好。二值形态学是在集合理论的基础上定义的。<strong>二值形态学最基本的运算腐蚀和膨胀。膨胀和腐蚀是相反的一对操作，膨胀就是求局部最大值的操作，腐蚀就是求局部最小值的操作</strong>。这里需要先介绍结构元素的概念。结构元素也被称为核元素，是进行形态学运算的模板。在腐蚀或者膨胀的时候，通过结构元素在图像区域按约定的规则进行运算，然后将变换结果输出，最后就得到形态运算后的结果。常见的结构元素是扁平型的，即结构元素高度为 0，也有非扁平型结构元素。结构元素的形状也有很多种，比如矩形的，十字交叉形的，菱形的，圆形的和椭圆形的。在选择结构元素时，需要<br>根据具体的目的来选择。结构元素一个重要的参考点，也称为锚点，决定了对图像进行形态操作时，以哪一个点为基准点。结构元素的大小也对最后的形态运算结果有很大影响。比如 3×3 的和 5×5 的结构元素。在进行形态学操作时，较大<br>的结构元素可以获得更好的处理效果，但是通常会耗时很多，相反，较小的结构元素处理起来会很快，但是处理效果欠佳。实际运用中需要通过一定的实验来确定灰度图像的腐蚀和膨胀过程可通过灰度图像和结构元素的二元函数关系计算出<br>来。Top-hat 则是灰度图像与灰度腐蚀、膨胀相结合的一种复合运算。设 F(x, y) 表示输入图像的二元函数，S(x, y) 为结构元素，SD 表示结构元素的定义域，则 S 对F 的腐蚀（ FS ）和膨胀（ F S ）运算定义如下：</p>
<p><img src="https://img-blog.csdnimg.cn/20200514085632902.png#pic_center" alt="image-20200505213016934"></p>
<p>另外，灰度图像的开运算、闭运算是灰度腐蚀、膨胀相结合的复合运算，定义如下，</p>
<p><img src="https://img-blog.csdnimg.cn/20200514085841495.png#pic_center" alt="image-20200505213413691"></p>
<p>灰度图像的开运算是结构元素对灰度图像先腐蚀再膨胀，该运算常可用来去除灰度图像中相对于结构元素较小的高灰度区域，而对较大的亮区域则不做响应；灰度图像闭运算即结构元素对灰度图像先膨胀再腐蚀。基于上述灰度图像的一些<br>基本运算，Top-hat 变换定义为</p>
<p><img src="https://img-blog.csdnimg.cn/20200514090141491.png#pic_center" alt="image-20200505213848350"></p>
<p>从上式中可知，Top-hat 的基本原理是先通过结构元素 S 对图像 F 进行开运算，以此提取出图像背景区域的灰度图像，然后将原图像与开运算的结果作差后得到Top-hat 运算的结果。因为开运算带来的结果是放大了局部低亮度的区域，因此，<br>从原图中减去开运算后的图，得到的效果图突出了比原图轮廓周围的区域更明亮的区域。 </p>
<p>采用 Top-hat 变换后的图像使光照不均图像得到了适当的灰度校正，有效地改善了缺陷图像的质量，便于后续图像处理。</p>
<h3 id="2：图像滤波："><a href="#2：图像滤波：" class="headerlink" title="2：图像滤波："></a>2：图像滤波：</h3><p>带钢图像采集、传输阶段以及现场环境中的某些干扰因素（如粉尘、水雾）等都会给采集到的图像带来一定程度的噪声因此采集到的图像需进行滤波处理。图像滤波根据操作空间的不同分为空域滤波和频域滤波。空域滤波是在图像所在像素空间进行的。频域滤波是把图像看成二维信号，在频域进行操作。</p>
<h4 id="1：均值滤波："><a href="#1：均值滤波：" class="headerlink" title="1：均值滤波："></a>1：均值滤波：</h4><p>均值滤波是一种领域增强算法，即用一个窗口在图像上滑动，然后求出该窗口覆盖区域内各个像素的灰度平均值，最后窗口中心位置的灰度值用图像区域的各个像素的均值来代替，常用的滤波窗口有 33、55，根据实际要处理的图像的要求选择不同的窗口模板。均值滤波用公式表示为：</p>
<script type="math/tex; mode=display">
g(x,y) = \frac{1}{m}\sum{f(k,l)}</script><p>g(x,y)为处理后的像素的灰度值， f(k,l) 为处理前模板内像素的灰度值，m为模板中包含当前像素在内的像素总个数。均值滤波不能很好的保护图像的细节，其在平滑消除噪声的同时将会使图像变得模糊，从而将严重改变缺陷区域的边缘灰度值，因此均值滤波往往应用在对图像处理要求不高的场合。 </p>
<h4 id="2中值滤波："><a href="#2中值滤波：" class="headerlink" title="2中值滤波："></a>2中值滤波：</h4><p>中值滤波是一种基于排序统计理论的有效抑制噪声的非线性滤波方法，具体操作过程是采用一个滑动窗口在图像上依次滑动，将窗口内各点的灰度值按大小顺序排列，用平滑模板邻域内各点的中值来代替模板中心像素点的灰度值用公式表示如下：<img src="https://img-blog.csdnimg.cn/20200514090221324.png#pic_center" alt="image-20200505223915851"></p>
<p>中值滤波在一定条件下能有效过滤掉脉冲噪声，但不适合细节太多的图像.</p>
<h4 id="3-双边滤波："><a href="#3-双边滤波：" class="headerlink" title="3 双边滤波："></a>3 双边滤波：</h4><p>双边滤波是一种非线性滤波方法。该滤波方法考虑空域信息和灰度相似性，达到保边缘和去噪的目的。在双边滤波器中，输出像素的值依赖于邻域像素值的加权值组合，公式如下：</p>
<p><img src="https://img-blog.csdnimg.cn/20200514090341954.png#pic_center" alt="image-20200505225242495"></p>
<p>而加权系数 lkjiw),,,( 取决于定义域核和值域核的乘积。两者相乘后，就会生产依赖于数据的双边滤波权重函数，如下所示</p>
<p>：</p>
<p><img src="https://img-blog.csdnimg.cn/20200514090356402.png#pic_center" alt="image-20200505225311070"></p>
<h3 id="3对缺陷进行分割："><a href="#3对缺陷进行分割：" class="headerlink" title="3对缺陷进行分割："></a>3对缺陷进行分割：</h3><h4 id="4-1-边缘检测"><a href="#4-1-边缘检测" class="headerlink" title="4.1 边缘检测"></a>4.1 边缘检测</h4><p>边缘检测是提取图像中灰度变化明显的部分，并据此确定缺陷区域的位置边缘检测首先检测出图像局部特征的不连续性，然后进行边缘连接，并通过封闭的边界实现缺陷区域的分割。 常用的边缘检测算子如 Sobel 算子、Roberts 算子、Prewitt 算子等。等有时间继续写，本人不善于进行时间管理。<br><img src="https://img-blog.csdnimg.cn/20200514090628429.jpg?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>图像处理</category>
      </categories>
      <tags>
        <tag>缺陷检测</tag>
      </tags>
  </entry>
  <entry>
    <title>Python自动部署码云</title>
    <url>/WYW1314/posts/47649.html</url>
    <content><![CDATA[<h1 id="Python自动部署码云："><a href="#Python自动部署码云：" class="headerlink" title="Python自动部署码云："></a>Python自动部署码云：</h1><h2 id="1：大概思路："><a href="#1：大概思路：" class="headerlink" title="1：大概思路："></a>1：大概思路：</h2><p>其实还是打开浏览器登录，只不过不用你自己再一步一步登录部署干啥。python代替你完成了这个过程。我用的谷歌浏览器，谷歌真香，用的人都说爽。</p>
<h2 id="2-下载chromedriver-exe文件："><a href="#2-下载chromedriver-exe文件：" class="headerlink" title="2:下载chromedriver.exe文件："></a>2:下载chromedriver.exe文件：</h2><p>首先打开你的谷歌浏览器。直接在地址栏输入：<a href="chrome://settings/help">chrome://settings/help</a>.这样会出来和你浏览器相对应的版本。如下图所示：<img src="https://img-blog.csdnimg.cn/20200413205439163.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>我的浏览器版本是81.0.4044.9.我下载的版本只要是81.0.4044.xxx就可以。下载文件的链接是：<a href="http://chromedriver.storage.googleapis.com/index.html。" target="_blank" rel="noopener">http://chromedriver.storage.googleapis.com/index.html。</a><br>下载完文件后把这个exe文件当到谷歌浏览器的路径下：我的路径为：<strong>E:\Chrome\Application</strong>。同时还要把这个文件放到你电脑上python的安装根目录下。</p>
<h2 id="3：开始编写程序："><a href="#3：开始编写程序：" class="headerlink" title="3：开始编写程序："></a>3：开始编写程序：</h2><p>这里我们用到的是python的以下这些库：</p>
<pre class=" language-lang-python"><code class="language-lang-python">from selenium import webdriver
from selenium.webdriver.common.alert import Alert
</code></pre>
<p>selenium是一个自动化测试工具，支持Firefox，Chrome等众多浏览器.</p>
<p>在爬虫中的应用主要是用来解决JS渲染的问题。</p>
<p>1.使用前需要安装这个第三方库，CMD下：</p>
<p>pip install selenium</p>
<p>安装完这个库就可以开始写代码了。</p>
<h3 id="1：模拟浏览器来到码云的登录界面，代码如下："><a href="#1：模拟浏览器来到码云的登录界面，代码如下：" class="headerlink" title="1：模拟浏览器来到码云的登录界面，代码如下："></a>1：模拟浏览器来到码云的登录界面，代码如下：</h3><pre class=" language-lang-python"><code class="language-lang-python"># 模拟浏览器打开到gitee登录界面
driver = webdriver.Chrome()
driver.get('https://gitee.com/login')
# 将窗口最大化
driver.maximize_window()
time.sleep(2)
</code></pre>
<p>这时候我们打开这个界面后需要输入账号和密码登录。<img src="https://img-blog.csdnimg.cn/20200413205549874.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>这时候我们会有一个疑惑，如何定位输入账号的地方的和输入密码的地方，这时候我们需要一点html的知识。就是如何让python定位到账号和密码的输入框。这个时候我们就可以谷歌浏览器的开发者工具来分析：</p>
<p>html输入框坑定是放在一个div里的。而且通过Id号很方便，来确定的可以从id号去确认。<u><del>我的html学的很垃圾。可能有些专业术语表达不清楚。</del></u></p>
<p>这里我们鼠标定位到输入框，右键，点击检查。<img src="https://img-blog.csdnimg.cn/20200413205638506.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>然后就会发现出现以下：</p>
<p><img src="https://img-blog.csdnimg.cn/20200413205731510.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>获得密码输入框的id也是和上面一样。我们得到了就可以用库里面的函数来定位到这个位置并且把你的账号和密码自动填入。代码如下：</p>
<pre class=" language-lang-python"><code class="language-lang-python"># 输入账号--通过html的id属性定位输入位置--改为你的账号
user_login = driver.find_element_by_id('user_login')
user_login.send_keys("你的账号")
# 输入密码--通过html的id属性定位输入位置--改为你的密码
driver.find_element_by_id('user_password').send_keys("你的密码")
</code></pre>
<p>接下来就是写程序点击登录这个按钮。那么这个按钮的位置如何确定呢。我们这里是采用<code>Xpath</code>来确定的。获取xpath也有一个快捷的方法。</p>
<p><img src="https://img-blog.csdnimg.cn/20200413205818923.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>依旧按上面的方法选中这一块，右键选择检查，定位到这一块的html代码。然后右键这一块。选择<code>copy full xpath.</code></p>
<p>如下图所示：<img src="https://img-blog.csdnimg.cn/20200413205859677.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>得到了<code>xpath</code>,接着写代码：</p>
<pre class=" language-lang-python"><code class="language-lang-python"># 点击登录按钮--通过xpath确定点击位置
driver.find_element_by_xpath(
    '/html/body/div[2]/div[2]/div[1]/div/div[2]/div/form[1]/div[2]/div/div/div[4]/input').click()
time.sleep(2)
</code></pre>
<p>Python方便的地方就是有相应的库函数。这里也是。</p>
<p>登陆完后，我们需要来到部署静态网页的页面：一般情况下，这个页面的URL链接为：’<a href="https://gitee.com/xxxxx/xxxxx/pages" target="_blank" rel="noopener">https://gitee.com/xxxxx/xxxxx/pages</a>‘</p>
<p>这时候我们从登录后的界面跳转到以上的URL链接界面。之后写程序点击部署那个按钮。代码如下：</p>
<pre class=" language-lang-python"><code class="language-lang-python"># 切换到gitee pages界面--改为you_gitee_id
driver.get('https://gitee.com/你项目的个人地址/项目名称/pages')
# 点击更新按钮--通过xpath确定点击位置
driver.find_element_by_xpath('/html/body/div[3]/div[2]/div/div[2]/div[1]/form/div[7]').click()
# 确认更新提示框--这个函数的作用是确认提示框
Alert(driver).accept()
</code></pre>
<p>之后等待部署，这里可以生成一个文件去记录你更新时间，代码如下;</p>
<pre class=" language-lang-Python"><code class="language-lang-Python"># 等待5秒更新
time.sleep(5)

# 这个print
print("成功")

# 脚本运行成功,退出浏览器
driver.quit()

fp = open("F:\log.txt", "a+")
now_time = time.strftime("%Y-%m-%d %H:%M:%S", time.localtime())
fp.write("auto update time:{0}\n".format(now_time))
fp.close()
</code></pre>
<p>这样就完成想象中的自动部署把。当然氪金一年99块钱也是能自动推送后部署的，但是贫穷限制了我的想象力。只有出此下策。哈哈哈。</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>爬虫</category>
      </categories>
      <tags>
        <tag>python 博客</tag>
      </tags>
  </entry>
  <entry>
    <title>齐次坐标的理解</title>
    <url>/WYW1314/posts/64390.html</url>
    <content><![CDATA[<h1 id="齐次坐标的理解"><a href="#齐次坐标的理解" class="headerlink" title="齐次坐标的理解"></a>齐次坐标的理解</h1><p>齐次坐标这个概念是在看图像仿射变换时候看到的。当初看的因为没接触过，就查了一下概念。<a id="more"></a><br>问题：两条平行线可以相交于一点 在欧氏几何空间，同一平面的两条平行线不能相交，这是我们都熟悉的一种场景。 然而，在透视空间里面，两条平行线可以相交，例如：火车轨道随着我们的视线越来越窄，最后两条平行线在无穷远处交于一点。  欧氏空间（或者笛卡尔空间）描述2D/3D几何非常适合，但是这种方法却不适合处理透视空间的问题（实际上，欧氏几何是透视几何的一个子集合），2维笛卡尔坐标可以表示为（x,y）。  如果一个点在无穷远处，这个点的坐标将会(∞,∞)，在欧氏空间，这变得没有意义。平行线在透视空间的无穷远处交于一点，但是在欧氏空间却不能，数学家发现了一种方式来解决这个问题。  方法：齐次坐标 简而言之，齐次坐标就是用N+1维来代表N维坐标  我们可以在一个2D笛卡尔坐标末尾加上一个额外的变量w来形成2D齐次坐标，因此，一个点(X,Y)在齐次坐标里面变成了（x,y,w），并且有 </p>
<p>X = x/w</p>
<p>Y = y/w</p>
<p>例如，笛卡尔坐标系下（1，2）的齐次坐标可以表示为（1，2，1），如果点（1，2）移动到无限远处，在笛卡尔坐标下它变为(∞,∞)，然后它的齐次坐标表示为（1，2，0），因为(1/0, 2/0) = (∞,∞)，我们可以不用”∞”来表示一个无穷远处的点了，哈哈。</p>
<p>为什么叫齐次坐标？</p>
<p>我们把齐次坐标转化为笛卡尔坐标的方法是前面n-1个坐标分量分别除以最后一个分量即可。</p>
<script type="math/tex; mode=display">
(x,y,w)\Leftrightarrow(\frac{x}{w},\frac{y}{w}).</script><p>然后齐次坐标有的一个性质就是齐次坐标的不变性。如：</p>
<script type="math/tex; mode=display">
(1,2,3)\Leftrightarrow(\frac{1}{3},\frac{2}{3})\\
(2,4,6)\Leftrightarrow(\frac{2}{6},\frac{4}{6})\Leftrightarrow(\frac{1}{3},\frac{2}{3})</script><p>所以齐次坐标具有规模不变性、</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>图像处理</category>
      </categories>
      <tags>
        <tag>齐次坐标</tag>
      </tags>
  </entry>
  <entry>
    <title>树莓派换源以及pip安装</title>
    <url>/WYW1314/posts/3101.html</url>
    <content><![CDATA[<h1 id="树莓派换源以及opencv还有opencv-contrib"><a href="#树莓派换源以及opencv还有opencv-contrib" class="headerlink" title="树莓派换源以及opencv还有opencv-contrib"></a>树莓派换源以及opencv还有opencv-contrib</h1><p>树莓派刷好系统后一般情况下都是国外的源，所以说及其的慢。可以换成国内的源。<a id="more"></a>其实很简单</p>
<h2 id="查看自己的系统版本"><a href="#查看自己的系统版本" class="headerlink" title="查看自己的系统版本"></a>查看自己的系统版本</h2><p>一般情况下刷好系统后，有各种各样的版本，在这里输入指令查看：</p>
<pre class=" language-lang-python"><code class="language-lang-python">lsb_release  -a
</code></pre>
<p><br>如下图所示：<br><br><img src="https://img-blog.csdnimg.cn/20200413213727373.jpg#pic_center" alt=""><br>可以看到codename为buster,所以一会换源的时候一定要记住。</p>
<h2 id="更改souces-list软件源"><a href="#更改souces-list软件源" class="headerlink" title="更改souces.list软件源"></a>更改souces.list软件源</h2><p>1:nano 编辑/etc/apt/目录下的sources.list<br>终端输入：sudo nano /etc/apt/sources.list<br><br><br><img src="https://img-blog.csdnimg.cn/20200413213852306.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""><br>我换的是清华大学镜像站的源，所以你换的时候输入下图的俩个网址就可以。<br>ctrl + o 保存。回车确认。ctrl + x 退出。</p>
<pre class=" language-lang-python"><code class="language-lang-python">deb http://mirrors.tuna.tsinghua.edu.cn/raspbian/raspbian/ buster main non-free contrib
deb-src http://mirrors.tuna.tsinghua.edu.cn/raspbian/raspbian/ buster main non-free contrib
</code></pre>
<h2 id="更换系统源"><a href="#更换系统源" class="headerlink" title="更换系统源"></a>更换系统源</h2><p>执行命令：</p>
<pre class=" language-lang-python"><code class="language-lang-python">sudo nano /etc/apt/sources.list.d/raspi.list
</code></pre>
<p><img src="https://img-blog.csdnimg.cn/20200413214127463.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""><br><br>输入以下网址：</p>
<pre class=" language-lang-python"><code class="language-lang-python">deb http://mirrors.tuna.tsinghua.edu.cn/raspberrypi/ buster main ui
</code></pre>
<h2 id="pip命令安装Opencv"><a href="#pip命令安装Opencv" class="headerlink" title="pip命令安装Opencv"></a>pip命令安装Opencv</h2><p>由于树莓派上是py3.7.所以使用pip命令安装时候要用pip3.<br>打开终端：</p>
<pre class=" language-lang-python"><code class="language-lang-python">sudo pip3  install opencv-python
</code></pre>
<p>这里我已经装完了。<br><img src="https://img-blog.csdnimg.cn/20200413214258552.png#pic_center" alt="">这里要注意你装之前一定要有numpy这个库。一般情况下刷完系统就有了。<br>装完之后你可以终端输入python3回车。输入import cv2<br><br>然后回车。如果出现以下错误：ImportError: libhdf5_serial.so.100: cannot open shared object file: No such file or directory.缺少若干依赖模块，<br>就把下面的所有来一遍就好了。</p>
<pre class=" language-lang-python"><code class="language-lang-python">sudo apt-get update #安装依赖库
sudo apt-get install libhdf5-dev
sudo apt-get install libatlas-base-dev
sudo apt-get install libjasper-dev
sudo apt-get install libqt4-test
sudo apt-get install libqtgui4
sudo apt-get update
</code></pre>
<p>之后再pip3，输入import cv2如果没有任何反应就证明好了。<br>安装其他包也是类似的。</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>树莓派</category>
      </categories>
      <tags>
        <tag>树莓派换源</tag>
      </tags>
  </entry>
  <entry>
    <title>汇编语言的学习记录</title>
    <url>/WYW1314/posts/31290.html</url>
    <content><![CDATA[<h1 id="汇编语言的学习笔记："><a href="#汇编语言的学习笔记：" class="headerlink" title="汇编语言的学习笔记："></a>汇编语言的学习笔记：</h1><p>汇编语言我看的是王爽写的《汇编语言第三版》，这本书写的挺好的。<br>链接: <a href="https://pan.baidu.com/s/1yoK7uoJhbmmg-o58mWkerQ" target="_blank" rel="noopener">https://pan.baidu.com/s/1yoK7uoJhbmmg-o58mWkerQ</a> 提取码: va7t<br>感觉在看汇编语言的前一部分时候感觉和学51单片机时候的寄存器一样。其实都还是相通的。由于最近还是因为疫情，而且过几天要开网课了。苦逼。所以在家只能学习。于是就学习汇编玩下。<a id="more"></a>然后我打算把我每天学习的记录一下。为了日后看的时候方便一点。</p>
<h2 id="机器语言："><a href="#机器语言：" class="headerlink" title="机器语言："></a>机器语言：</h2><p>机器语言就是0和1，但机器语言实在过于复杂，所以以前的程序猿真的苦逼，要写一堆0和1.所以汇编语言就产生了。与现在的高级语言的效率相比。汇编语言的效率是最快的。</p>
<h2 id="编译器作用"><a href="#编译器作用" class="headerlink" title="编译器作用"></a>编译器作用</h2><p>编译器的作用就是将汇编指令转换为机器指令。</p>
<h2 id="汇编语言的组成"><a href="#汇编语言的组成" class="headerlink" title="汇编语言的组成"></a>汇编语言的组成</h2><p>汇编语言通常有以下3类指令：</p>
<p>(1):汇编指令：机器码的助记符，有对应的机器码。<br>(2):伪指令：没有对应的机器码，由编译器执行，计算机并不执行。<br>(3):其他符号：如+，-，*，/等，由编译器识别，没有对应的机器码。</p>
<h2 id="指令和数据"><a href="#指令和数据" class="headerlink" title="指令和数据"></a>指令和数据</h2><p>指令和数据在内存或者磁盘上存储的时候是一样的。都是二进制信息。但是CPU可以把一串二进制信息看作数据来处理，也可以将其看作指令处理</p>
<h2 id="存储单元"><a href="#存储单元" class="headerlink" title="存储单元"></a>存储单元</h2><p>这个概念是比较课本的概念。存储器被划分为若干个存储单元。每个存储单元从0开始编号。也就是从0到127.一个存储器有128个存储单元，一个存储单元可以存储一个Byte.即8个二进制位。</p>
<h2 id="CPU对数据进行读写"><a href="#CPU对数据进行读写" class="headerlink" title="CPU对数据进行读写"></a>CPU对数据进行读写</h2><p>CPU要想进行数据的读写，必须和外部器件进行交互。必须有以下三类信息的交互:<br><br>1：存储单元的地址<br><br>2：器件的选择，读或写的命令。<br><br>3：读或写的数据(数据信息)<br><br>CPU对数据进行读取的过程如下所示：<br><img src="http://q5zz73y15.bkt.clouddn.com/XL%5B%7BX%7B9QVNBE0A%29LW_LP5$V.png" alt=""><br>(1):cpu通过地址总线将地址信息3发出<br><br>(2):cpu通过控制线发出内存读命令，选中存储器芯片，并通知他。从中读取数据。<br><br>(3):存储器将3号单元的数据8通过数据线送入CPU。写操作也是类似的。</p>
<h2 id="地址总线"><a href="#地址总线" class="headerlink" title="地址总线"></a>地址总线</h2><p>CPU通过地址总线来指定存储器单元的。假如一个CPU有10根地址总线，那么就是2的10次方个。也就是从0到1023.<br><img src="http://q5zz73y15.bkt.clouddn.com/666.png" alt=""><br>如上图所示这个就是10根地址线的CPU向内存发出地址信息11时10根地址线上传输的二进制信息。规定从上到下就是从最低位到最高位。</p>
<h2 id="数据总线"><a href="#数据总线" class="headerlink" title="数据总线"></a>数据总线</h2><p>CPU与内存或其他器件之间的数据传送是通过数据总线来进行的。<br>八根数据总线一次可传送一个八位二进制数据(也就是一个字节)，16根数据总线一次可以传送俩个字节。</p>
<h2 id="控制总线"><a href="#控制总线" class="headerlink" title="控制总线"></a>控制总线</h2><p>CPU对外部器件的控制是通过控制总线来进行了。在这里控制总线是个总称，控制总线的宽度决定了CPU对外部器件的控制能力。内存读或写命令是由几根控制总线综合发出的。其中有一根叫做“读信号输出”的控制线负责有CPU向外传送读信号，CPU向该控制线上输出低电平表示将要读取数据，有一根称为“写信号输出”的控制线负责传送写信号。</p>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>汇编语言</category>
      </categories>
      <tags>
        <tag>汇编知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>PCA主成分分析：</title>
    <url>/WYW1314/posts/53510.html</url>
    <content><![CDATA[<h1 id="PCA主成分分析："><a href="#PCA主成分分析：" class="headerlink" title="PCA主成分分析："></a>PCA主成分分析：</h1><p>主成分分析是一种矩阵的压缩算法，在减少矩阵维数的同时尽可能的保留原矩阵的信息，简单来说就是将 n×m的矩阵转换成n×k的矩阵（通常k是小于m的），仅保留矩阵中所存在的主要特性，从而可以大大节省空间和数据量。<a id="more"></a>然后假期学习到图像主成分分析的书上直接给出了程序实现的过程，而其中原理并不是特别清楚。去网上找别人写的博客去看的时候，感觉都写的过于片面。然后我就想在这些基础之上总结一下。</p>
<p><strong>PCA数学原理分析</strong>：</p>
<p><a href="http://blog.codinglabs.org/articles/pca-tutorial.html" target="_blank" rel="noopener">PCA数学原理分析</a></p>
<p>分析这篇文章写的很好：可以参考这篇文章。</p>
<p>我们生活所面对的数据都可以完全被抽象为一组向量。</p>
<h2 id="内积和-投影："><a href="#内积和-投影：" class="headerlink" title="内积和 投影："></a>内积和 投影：</h2><p>两个维数相同的向量的内积被定义为：</p>
<script type="math/tex; mode=display">
(a_1,a_2 ... a_n)*(b_1,b_2...b_n)=a_1b_1+a_2b_2+...+a_nb_n</script><p><img src="https://img-blog.csdnimg.cn/20200413212512883.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>设A=(x1,y1),B=(x2,y2).好，现在我们从A点向B所在直线引一条垂线。我们知道垂线与B的交点叫做A在B上的投影，再设A与B的夹角是a，则投影的矢量长度为</p>
<script type="math/tex; mode=display">
|A|cosa</script><p>接下来内积还有另外一种表示方法：</p>
<script type="math/tex; mode=display">
A*B=|A|*|B|*cosa</script><p>也就是说，<strong>设向量B的模为1，则A与B的内积值等于A向B所在直线投影的矢量长度</strong>！</p>
<h2 id="基："><a href="#基：" class="headerlink" title="基："></a>基：</h2><p><img src="https://img-blog.csdnimg.cn/2020041321255810.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>这个红色线段的向量我们通常都直接认为是(3,2).但实际上这样说只是相对于当前的这个坐标系来说。所以来说并不是特别准确。</p>
<p>实际要准确表示这个向量的话，应该按照下面的去表示：</p>
<script type="math/tex; mode=display">
3(1,0)^T+2(0,1)^T</script><p>此处(1,0)和(0,1)叫做二维空间中的一组基。所以，<strong>要准确描述向量，首先要确定一组基，然后给出在基所在的各个直线上的投影值，就可以了</strong>。只不过我们经常省略第一步，而默认以(1,0)和(0,1)为基。我们之所以默认选择(1,0)和(0,1)为基，当然是比较方便，因为它们分别是x和y轴正方向上的单位向量，因此就使得二维平面上点坐标和向量一一对应，非常方便。但实际上任何两个线性无关的二维向量都可以成为一组基，所谓线性无关在二维平面内可以直观认为是两个不在一条直线上的向量。</p>
<p>，(1,1)和(-1,1)也可以成为一组基。一般来说，我们希望基的模是1，因为从内积的意义可以看到，如果基的模是1，那么就可以方便的用向量点乘基而直接获得其在新基上的坐标了！实际上，对应任何一个向量我们总可以找到其同方向上模为1的向量，只要让两个分量分别除以模就好了。这一步通常在线性代数也叫做单位化。所以上面的基可以变为：</p>
<script type="math/tex; mode=display">
(\frac{1}{\sqrt{2}},\frac{1}{\sqrt{2}})和(-\frac{1}{\sqrt{2}},\frac{1}{\sqrt{2}}）</script><p>现在我们要想获得(3,2)在新的基上的坐标，我们只需要分别计算(3,2)与新的基的内积即可，</p>
<p>所以可以得到：</p>
<script type="math/tex; mode=display">
(\frac{5}{\sqrt{2}},\frac{-1}{\sqrt{2}})</script><p><img src="https://img-blog.csdnimg.cn/20200413212634721.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>我们所选的大多数情况下选的基都是正交的。</p>
<h2 id="基变换的矩阵表示"><a href="#基变换的矩阵表示" class="headerlink" title="基变换的矩阵表示"></a>基变换的矩阵表示</h2><p>因为进行基变换的方法坐标分别与基做内积运算。所以我们可以用矩阵去表示这种变换，我们写的时候通常将原来的坐标写为列向量的形式。</p>
<script type="math/tex; mode=display">
\begin{pmatrix} 1/\sqrt{2} & 1/\sqrt{2} \\ -1/\sqrt{2} & 1/\sqrt{2} \end{pmatrix} \begin{pmatrix} 3 \\ 2 \end{pmatrix} = \begin{pmatrix} 5/\sqrt{2} \\ -1/\sqrt{2} \end{pmatrix}</script><p>如果我们有m个二维向量，只要将二维向量按列排成一个两行m列矩阵，然后用“基矩阵”乘以这个矩阵，就得到了所有这些向量在新基下的值。</p>
<p><strong>一般的，如果我们有M个N维向量，想将其变换为由R个N维向量表示的新空间中，那么首先将R个基按行组成矩阵A，然后将向量按列组成矩阵B，那么两矩阵的乘积AB就是变换结果，其中AB的第m列为A中第m列变换后的结果</strong>。</p>
<p>矩阵的相乘在看完那篇博客后好像明白他们的意义。其实矩阵变换无非就是<strong>两个矩阵相乘的意义是将右边矩阵中的每一列列向量变换到左边矩阵中每一行行向量为基所表示的空间中去</strong>。</p>
<h2 id="协方差矩阵及其表示："><a href="#协方差矩阵及其表示：" class="headerlink" title="协方差矩阵及其表示："></a>协方差矩阵及其表示：</h2><p>如果进行<strong>矩阵变换的时候基的数量少于向量本身的维数的时候我们就达到了降维的目的</strong>。但是如何选取基又是一个问题。看了别人的博客写的后感觉这种变换的思想真的巧妙。</p>
<p>选取一个合适的基的意思是变换后的坐标尽量分散。而分散的程度我们在数学上通常使用方差来表示，一个字段的方差可以看做是每个元素与字段均值的差的平方和的均值，</p>
<script type="math/tex; mode=display">
Var(a)=\frac{1}{m}\sum_{i=1}^m{(a_i-\mu)^2}</script><p>在这里为了<strong>简化运算我们通常提前对数据进行处理，每个数据减去这个字段的均值。</strong></p>
<p>处理过程很简单。举个例子：</p>
<script type="math/tex; mode=display">
1,2,3,4,5这五个数据的均值为15/5=3所以每个数据都减去3.所以处理后的数据为：-2，-1,0，1,2.这五个数据均值一定为0.</script><p>这样处理后，</p>
<script type="math/tex; mode=display">
这样\mu可以为0了。</script><p><strong><em>所以寻找一个基，使得所有数据变换为这个基上的坐标表示后，方差值最大。</em></strong></p>
<p>对于上面二维降成一维的问题来说，找到那个使得方差最大的方向就可以了。不过对于更高维，还有一个问题需要解决。考虑三维降到二维问题。与之前相同，首先我们希望找到一个方向使得投影后方差最大，这样就完成了第一个方向的选择，继而我们选择第二个投影方向。</p>
<p>如果我们还是单纯只选择方差最大的方向，很明显，这个方向与第一个方向应该是“几乎重合在一起”，显然这样的维度是没有用的，因此，应该有其他约束条件。从直观上说，让两个字段尽可能表示更多的原始信息，我们是不希望它们之间存在（线性）相关性的，因为相关性意味着两个字段不是完全独立，必然存在重复表示的信息。</p>
<p>数学上可以用两个字段的协方差表示其相关性，由于已经让每个字段均值为0，协方差原始公式为：<img src="https://img-blog.csdnimg.cn/20200413212728448.png#pic_center" alt=""></p>
<p>则协方差的公式如下：</p>
<script type="math/tex; mode=display">
Cov(a,b)=\frac{1}{m}\sum_{i=1}^m{a_ib_i}</script><p>可以看到，在字段均值为0的情况下，两个字段的协方差简洁的表示为其内积除以元素数m。当协方差为0时，表示两个字段完全独立。为了让协方差为0，我们选择第二个基时只能在与第一个基正交的方向上选择。因此最终选择的两个方向一定是正交的。为什么正交的话？因为线性代数里面也说正交的话内积为0，所以协方差就为0.</p>
<p><strong>将一组N维向量降为K维（K大于0，小于N），其目标是选择K个单位（模为1）正交基，使得原始数据变换到这组基上后，各字段两两间协方差为0，而字段的方差则尽可能大（在正交的约束下，取最大的K个方差）</strong></p>
<p>上面就是我们要得到的目标，但是就是上面的这个结论并不能告诉我们要具体怎么去做。所以说数学的思想真的巧妙。</p>
<p>我们设有a和b俩个字段。</p>
<script type="math/tex; mode=display">
X=\begin{pmatrix} a_1 & a_2 & \cdots & a_m \\ b_1 & b_2 & \cdots & b_m \end{pmatrix}</script><p>然后我们用X乘以X的转置，并乘上系数1/m：</p>
<script type="math/tex; mode=display">
\frac{1}{m}XX^\mathsf{T}=\begin{pmatrix} \frac{1}{m}\sum_{i=1}^m{a_i^2} & \frac{1}{m}\sum_{i=1}^m{a_ib_i} \\ \frac{1}{m}\sum_{i=1}^m{a_ib_i} & \frac{1}{m}\sum_{i=1}^m{b_i^2} \end{pmatrix}</script><p>然后神奇的事情发生了，主对角线上是方差，斜对角线是协方差。</p>
<p><strong>设我们有m个n维数据记录，将其按列排成n乘m的矩阵X，设</strong></p>
<script type="math/tex; mode=display">
C=\frac{1}{m}XX^\mathsf{T}</script><p><strong>，则C是一个对称矩阵，其对角线分别个各个字段的方差，而第i行j列和j行i列元素相同，表示i和j两个字段的协方差</strong></p>
<h2 id="协方差矩阵对角化："><a href="#协方差矩阵对角化：" class="headerlink" title="协方差矩阵对角化："></a>协方差矩阵对角化：</h2><p>为什么要将协方差矩阵对角化呢。首先我们了解一下相似矩阵。设A和B为n阶矩阵如果存在n阶可逆矩阵P，使得：</p>
<script type="math/tex; mode=display">
P^{-1}AP=B</script><p>我们就称A相似于B，或者说A和B相似。矩阵相似具有非常多好用的性质：</p>
<p><strong>A和B有相等的行列式。A和B有相等的迹。</strong></p>
<p>相似矩阵具有许多共同的性质，对于N阶矩阵。我们希望与A相似的矩阵中寻找一个较简单的矩阵。所以我们考虑n阶矩阵是否与一个对角矩阵相似的问题的时候。这时候就用到了矩阵对角化。</p>
<p>设原始数据矩阵X对应的协方差矩阵为C，而P是一组基按行组成的矩阵，设Y=PX，则Y为X对P做基变换后的数据。设Y的协方差矩阵为D，我们推导一下D与C的关系：</p>
<script type="math/tex; mode=display">
\begin{array}{l l l} D & = & \frac{1}{m}YY^\mathsf{T} \\ & = & \frac{1}{m}(PX)(PX)^\mathsf{T} \\ & = & \frac{1}{m}PXX^\mathsf{T}P^\mathsf{T} \\ & = & P(\frac{1}{m}XX^\mathsf{T})P^\mathsf{T} \\ & = & PCP^\mathsf{T} \end{array}</script><p>现在事情很明白了！我们要找的P不是别的，而是能让原始协方差矩阵对角化的P。换句话说，优化目标变成了<strong>寻找一个矩阵P，满足</strong></p>
<script type="math/tex; mode=display">
PCP^\mathsf{T}</script><hr>
<p><strong>是一个对角矩阵，并且对角元素按从大到小依次排列，那么P的前K行就是要寻找的基，用P的前K行组成的矩阵乘以X就使得X从N维降到了K维并满足上述优化条件</strong>。</p>
<p>，在线性代数上，实对称矩阵有一系列非常好的性质：</p>
<p>1）实对称矩阵不同特征值对应的特征向量必然正交。</p>
<script type="math/tex; mode=display">
2）设特征向量\lambda重数为r，则必然存在r个线性无关的特征向量对应于\lambda\\

因此可以将这r个特征向量单位正交化。</script><p>一个n行n列的实对称矩阵一定可以找到n个单位正交特征向量，设这n个特征向量为:</p>
<script type="math/tex; mode=display">
e_1,e_2,\cdots,e_n</script><p>则对协方差矩阵C有如下结论：</p>
<script type="math/tex; mode=display">
E^\mathsf{T}CE=\Lambda=\begin{pmatrix} \lambda_1 & & & \\ & \lambda_2 & & \\ & & \ddots & \\ & & & \lambda_n \end{pmatrix}</script><script type="math/tex; mode=display">
P=E^\mathsf{T}</script><p>矩阵P就已经得到了。</p>
<p>P是协方差矩阵的特征向量单位化后按行排列出的矩阵，其中每一行都是C的一个特征向量。如果设P按照特征值的从大到小，将特征向量从上到下排列，则用P的前K行组成的矩阵乘以原始数据矩阵X，就得到了我们需要的降维后的数据矩阵Y。</p>
<h2 id="PCA算法"><a href="#PCA算法" class="headerlink" title="PCA算法"></a>PCA算法</h2><p>总结一下PCA的算法步骤：</p>
<p>设有m条n维数据。</p>
<p>1）将原始数据按列组成n行m列矩阵X</p>
<p>2）将X的每一行（代表一个属性字段）进行零均值化，即减去这一行的均值</p>
<p>3）求出协方差矩阵</p>
<script type="math/tex; mode=display">
C=\frac{1}{m}XX^\mathsf{T}</script><p>4）求出协方差矩阵的特征值及对应的特征向量</p>
<p>5）将特征向量按对应特征值大小从上到下按行排列成矩阵，取前k行组成矩阵P</p>
<p>6）Y=PX即为降维到k维后的数据</p>
<p>代码如下：</p>
<pre class=" language-lang-python"><code class="language-lang-python">from PIL import Image
from numpy import *
from pylab import *
from imtools import get_imlist
import pickle
def pca(X):
    """主成分分析
    输入：矩阵X其中该矩阵中存储训练数据，每一行为一条训练数据
    返回：投影矩阵（按照维度的重要性排序），方差和均值
    """
    num_data,dim = X.shape#获取维数
    #数据中心化
    mean_x = X.mean(axis=0)#axis = 0：压缩行，对各列求均值，返回 1* n 矩阵axis =1 ：压缩列，对各行求均值，返回 m *1 矩阵
    X = X - mean_x
    if dim > num_data:
        M = dot(X,X.T) #PCA-使用紧致技巧得到协方差矩阵
        e,EV = linalg.eigh(M)#计算特征值和特征向量
        tmp = dot(X.T,EV).T#这就是紧致技巧
        V  = tmp[::-1]#由于最后的特征向量就是我们需要的，所以将其逆转
        S = sqrt(e)[::-1]#由于特征值是按照递增顺序排列的，所以需要将其逆转。
        for i in range(V.shape[1]):
            V[:,i] /= S
    else:
        U,S,V = linalg.svd(X)
        V = V[:num_data]#仅仅返回num_data维的数据才合理
    return V,S,mean_x#返回投影矩阵，方差和均值
imlist = get_imlist(r'..\image\1.jpg','.jpg')
im = array(Image.open(imlist[0]))#打开一幅图像，获得其大小
m,n = im.shape[0:2]
imnum = len(imlist)
#创建矩阵，保存所有压平后的图像数据
immatrix = array([array(Image.open(im)).flatten() for im in imlist],'f')
#执行PCA操作
V,S,immean = pca(immatrix)
#显示一些图像（均值图像和前10个模式）
figure()
gray()
subplot(2,5,1)#m和n代表在一个图像窗口中显示m行n列个图像
imshow(immean.reshape(m,n))
for i in range(9):
    subplot(2,5,i+2)
    imshow(V[i].reshape(m,n))
</code></pre>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>图像处理</category>
      </categories>
      <tags>
        <tag>PCA降维</tag>
      </tags>
  </entry>
  <entry>
    <title>Harris角点检测</title>
    <url>/WYW1314/posts/48085.html</url>
    <content><![CDATA[<h1 id="Harris角点检测"><a href="#Harris角点检测" class="headerlink" title="Harris角点检测"></a>Harris角点检测</h1><h2 id="角点的定义："><a href="#角点的定义：" class="headerlink" title="角点的定义："></a>角点的定义：</h2><p>角点检测算法基本思想是使用一个固定窗口（取某个像素的一个邻域窗口）在图像上进行<strong>任意方向</strong>上的滑动，比较滑动前与滑动后两种情况，窗口中的像素灰度变化程度，<strong>如果存在任意方向上的滑动，都有着较大灰度变化，那么我们可以认为该窗口中存在角点。</strong><br><a id="more"></a><br>角点：最直观的印象就是在水平和竖直两个方向变化均较大的两个点，即X,Y方向梯度都较大</p>
<p>边缘：仅在水平或竖直方向有较大的变化量，即X、Y方向梯度只有一个较大</p>
<p>平坦区域：在水平和竖直方向的变化量均较小，即X、Y方向梯度都较小</p>
<p><strong>在特征点检测中经常提出尺度不变、旋转不变、抗噪声影响等，这些是判断特征点是否稳定的指标。</strong></p>
<p><strong>性能较好的角点：</strong></p>
<ol>
<li><strong>检测出图像中“真实”的角点</strong></li>
<li><strong>准确的定位性能</strong></li>
<li><strong>很高的重复检测率</strong></li>
<li><strong>噪声的鲁棒性</strong></li>
<li><strong>较高的计算效率</strong></li>
</ol>
<p>今天看到角点检测的时候真的是一脸蒙蔽，服了，书上直接给出的公式。整的我直接懵了。参考晚上其他大佬写的博客后逐渐明白了一点</p>
<p>书上给的公式如下：</p>
<script type="math/tex; mode=display">
E(u,v)=\sum_{x,y}w(x,y) \left[]
 \begin{matrix}
  Ix^2 & IxIy\\
  IxIy & Iy^2
  \end{matrix}
  \right] \tag{B}</script><p>w(x,y)是窗口函数，最简单情形就是窗口WW内的所有像素所对应的w权重系数均为1.但有时候，我们会将w(x,y)函数设置为以窗口WW中心为原点的二元正态分布。如果窗口WW中心点是角点时，移动前与移动后，该点在灰度变化贡献最大；而离窗口WW中心(角点)较远的点，这些点的灰度变化几近平缓，这些点的权重系数，可以设定小值，以示该点对灰度变化贡献较小，那么我们自然而然想到使用二元高斯函数来表示窗口函数；</p>
<p><img src="https://img-blog.csdnimg.cn/20200413211148346.png#pic_center" alt=""></p>
<h2 id="Harris矩阵推导过程"><a href="#Harris矩阵推导过程" class="headerlink" title="Harris矩阵推导过程:"></a>Harris矩阵推导过程:</h2><p>使用一个固定窗口在图像上进行任意方向上的滑动。滑动分别为u和v.</p>
<p>比较滑动前与滑动后俩种情况。所以可以得到以下公式：</p>
<script type="math/tex; mode=display">
E(u,v)=\sum_{x,y}w(x,y)[I(x+u,y+v)-I(x,y)]^2</script><p>   高数中将这个式子进行泰勒展开：</p>
<script type="math/tex; mode=display">
f(x+u,y+v)\approx f(x,y)+ufx(x,y)+vfy(x,y)</script><script type="math/tex; mode=display">
所以\sum[I(x+u,y+v)-I(x,y)]^2=\\\sum[I(x,y)+uIx+vIy-I(x,y)]^2\\=\\\sum(u^2Ix^2)+2uvIxIy+v^2Iy^2\\=\\\sum[u,v] \left[
 \begin{matrix}
  Ix^2 & IxIy\\
  IxIy & Iy^2
  \end{matrix}
  \right] \tag{B}
  \left[
  \begin{matrix}
  u\\
  v\\
  \end{matrix}
  \right]</script><ul>
<li>[u,v][u,v]是窗口WW的偏移量；</li>
<li>(x,y)(x,y)是窗口WW所对应的像素坐标位置，窗口有多大，就有多少个位置；</li>
<li>I(x,y)是像素坐标位置(x,y)的图像灰度值；</li>
<li>I(x+u,y+v)是像素坐标位置(x+u,y+v)的图像灰度值；</li>
</ul>
<p> Ix,Iy分别为窗口内像素点(x,y)在x方向上和y方向上的梯度值。  其中<strong>窗口函数（权重矩阵）</strong>可以是平坦的，通常为高斯滤波器Gσ：</p>
<p>设:</p>
<script type="math/tex; mode=display">
Mi =\left[
 \begin{matrix}
  Ix^2 & IxIy\\
  IxIy & Iy^2
  \end{matrix}
  \right]\\
  M = \sum_{x,y}w(x,y) * Mi</script><p>所以可以得到卷积：M</p>
<p>该卷积的目的是得到MI在周围像素上的局部平均。<strong>矩阵M又称为Harris矩阵。</strong>W 的宽度决定了在像素x 周围的感兴趣区域。</p>
<p>根据上述表达式，当窗口在平坦区域上移动，可以想象得到，灰度不会发生什么变换。E(u,v)=0；如果窗口处在纹理比较丰富的区域上滑动，那么灰度变化会很大。算法最终思想就是计算灰度发生较大变化时所对应的位置，当然这个较大是指任意方向上的滑动，并非单指某个方向。</p>
<p>接下来我们对矩阵M的特征值进行分析。设矩阵M的特征值为λ1,λ2.</p>
<p>则有以下三种情况：</p>
<p><img src="https://img-blog.csdnimg.cn/20200413211353226.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQ0MjE5ODgz,size_16,color_FFFFFF,t_70#pic_center" alt=""></p>
<p>具体为什么有这三种情况以下就是推导过程：</p>
<p>我们可以将E(u,v)近似为二项函数：</p>
<script type="math/tex; mode=display">
E(u,v)=Au^2+2Cuv+Bv^2=\left[
 \begin{matrix}
  A & C\\
  C & A\\
  \end{matrix}
  \right]\\
A=\sum_{x,y}w(x,y)*Ix^2\\
B=\sum_{x,y}w(x,y)*Iy^2\\
C=\sum_{x,y}w(x,y)*IxIy</script><p>二次项函数本质上就是一个椭圆函数。椭圆的长和宽是由MM的特征值λ1,λ2决定的(椭圆的长短轴正是矩阵MM特征值平方根的倒数)，椭圆的方向是由M的特征向量决定的，椭圆方程为：</p>
<script type="math/tex; mode=display">
\left[
 \begin{matrix}
  u & v\\
  \end{matrix}
  \right]*M*\left[
 \begin{matrix}
   u\\
   v\\
  \end{matrix}
  \right]=1</script><p>虽然我们利用E(u,v)来描述角点的基本思想，然而最终我们仅仅使用的是矩阵M。让我们看看矩阵M形式，是不是跟协方差矩阵形式很像，像归像，但是还是有些不同，哪儿不同？一般协方差矩阵对应维的随机变量需要减去该维随机变量的均值：</p>
<p>把Ix,Iy看成两个字段，假设窗口内有m个像素点，也就是等价于有m个样本，我们先计算每个字段的均值：</p>
<script type="math/tex; mode=display">
\overline{Ix}=\sum_{i}^mIxi\\
\overline{Iy}=\sum_{i}^mIyi</script><p>我们仍然使用(Ixi,Iyi)表示样本(Ixi,Iyi)去均值后的值，则由这m个样本组成的矩阵：</p>
<script type="math/tex; mode=display">
x=\left[
 \begin{matrix}
   Ix1 & Ix2 & ... & Ixm \\
   Iy1 & Iy2 & ... & Iym\\
  \end{matrix}
  \right]</script><p>则对应协方差矩阵可以写成：</p>
<script type="math/tex; mode=display">
C=\frac{1}{m}*X*X^T=\frac{1}{m}*\sum_{i=1}\left[
 \begin{matrix}
  Ix^2 & IxIy\\
  IxIy & Iy^2
  \end{matrix}
  \right]</script><p>但矩阵M中并没有这样做，所以在矩阵M里，我们先进行各维的均值化处理，那么各维所对应的随机变量的均值为0，协方差矩阵就大大简化了，简化的最终结果就是矩阵M，是否明白了(注意为了简化运算，我们先假设M矩阵中的权重系数w(x,y)=1，并且省略掉了求均值.我们的目的是分析数据的主要成分</p>
<p>如果我们对协方差矩阵M进行对角化，很明显，其对角线就是各个字段的方差，这点大家应该明白吧？不明白的话可以复习下PCA原理。</p>
<ul>
<li>如果两个字段(Ix,Iy)所对应的特征值都比较大，说明什么？ 像素点的梯度分布比较散，梯度变化程度比较大，符合角点在窗口区域的特点；</li>
<li>如果是平坦区域，那么像素点的梯度所构成的点集比较集中在原点附近，因为窗口区域内的像素点的梯度幅值非常小，此时矩阵M的对角化的两个特征值比较小；</li>
<li>如果是边缘区域，在计算像素点的x、y方向上的梯度时，边缘上的像素点的某个方向的梯度幅值变化比较明显，另一个方向上的梯度幅值变化较弱，其余部分的点都还是集中原点附近，这样MM对角化后的两个特征值理论应该是一个比较大，一个比较小，当然对于边缘这种情况，可能是呈45°的边缘，致使计算出的特征值并不是都特别的大，总之跟含有角点的窗口的分布情况还是不同的。</li>
</ul>
<p>因此可以得出下列结论：</p>
<ul>
<li><p>特征值都比较大时，即窗口中含有角点；</p>
</li>
<li><p>特征值一个较大，一个较小，窗口中含有边缘；</p>
</li>
<li><p>特征值都比较小，窗口处在平坦区域；</p>
<p>那么根据书上又提出了一个不用实际计算特征值的方法。也就是度量角点响应：</p>
<p>对每一个窗口计算得到一个分数R，根据R的大小来判定窗口内是否存在harris特征角。分数R根据下面公式计算得到：</p>
</li>
</ul>
<script type="math/tex; mode=display">
R=det(M)-k(trace(M))^2\\</script><p>  其中：</p>
<script type="math/tex; mode=display">
det(M)=\lambda1*\lambda2\\
  trace(M)=\lambda1+\lambda2\\</script><p>  另外我在别的地方看到简化后的R为：</p>
<script type="math/tex; mode=display">
R=[(Ix^2+Iy^2)-(IxIy)^2]/(Ix^2+Iy^2)</script><h3 id="得到Harris角点的步骤"><a href="#得到Harris角点的步骤" class="headerlink" title="得到Harris角点的步骤"></a>得到Harris角点的步骤</h3><p>所以得到Harris角点的步骤为：</p>
<p>1、计算图像I(x,y)I(x,y)在x和y两个方向的梯度Ix,Iy;</p>
<p>2、计算图像两个方向梯度的乘积;</p>
<p>3、使用高斯函数对第二步算出的数进行高斯加权，计算中心点为(x,y)的窗口W对应的矩阵M</p>
<p>4、计算每个像素点(x,y)处的(x,y)处的Harris响应值R；</p>
<p>5：过滤大于某一阈值t的R值；</p>
<h3 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h3><p>python代码如下：</p>
<pre class=" language-lang-python"><code class="language-lang-python">from scipy.ndimage import filters
from PIL import Image
from pylab import *
from imtools import imresize
np.set_printoptions(threshold=np.inf)#解决printf打印矩阵不完全的问题
def harris_response(im,sigma=3):
    """计算图像的harris响应函数"""
    #计算导数
    imx = zeros(im.shape)
    imx = filters.gaussian_filter(im,(sigma,sigma),(0,1),imx)
    imy = zeros(im.shape)
    imy = filters.gaussian_filter(im, (sigma, sigma), (1, 0), imy)
    #计算Harris的各个分量
    wxx = filters.gaussian_filter(imx*imx,sigma)
    wxy = filters.gaussian_filter(imx*imy,sigma)
    wyy = filters.gaussian_filter(imy*imy,sigma)
    #计算像素的角点响应函数
    return (wxx*wyy - 2*wxy)/(wxx + wyy)
def get_harris_points(harrism,min_dist = 10,thresold = 0.1):
    """从一幅Harrisim响应中返回角点，min_dist为分割角点和图像边界的最少像素数目"""
    corner_thsold = harrism.max()*thresold
    harrism_t = (harrism > corner_thsold) * 1
    #得到候选点的坐标
    coords = array(harrism_t.nonzero()).T#返回非零值的坐标的矩阵
    #他们的Harris响应值
    candidate_values = [harrism[c[0],c[1]] for c in coords]
    #对候选点进行harris响应值进行排序
    index = argsort(candidate_values)[::-1]#将x中的元素从小到大排列，提取其对应的index(索引)，然后输出到y
    #将可行点的位置保存在数组里
    allowed_locations = zeros(harrism.shape)
    allowed_locations[min_dist:-min_dist,min_dist:-min_dist] = 1
    #按照min_distance原则，选择最佳harris点
    filters_coords = []
    for i in index:
            if allowed_locations[coords[i,0],coords[i,1]] == 1:
                filters_coords.append(coords[i])
                allowed_locations[(coords[i,0]-min_dist):(coords[i,0]+min_dist),(coords[i,1]-min_dist):coords[i,1]+min_dist] = 0
    return filters_coords
</code></pre>
<script>
        document.querySelectorAll('.github-emoji')
          .forEach(el => {
            if (!el.dataset.src) { return; }
            const img = document.createElement('img');
            img.style = 'display:none !important;';
            img.src = el.dataset.src;
            img.addEventListener('error', () => {
              img.remove();
              el.style.color = 'inherit';
              el.style.backgroundImage = 'none';
              el.style.background = 'none';
            });
            img.addEventListener('load', () => {
              img.remove();
            });
            document.body.appendChild(img);
          });
      </script>]]></content>
      <categories>
        <category>图像处理</category>
      </categories>
      <tags>
        <tag>Harris</tag>
      </tags>
  </entry>
</search>
