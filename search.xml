<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Harris角点检测</title>
    <url>/2020/02/19/Harris%E8%A7%92%E7%82%B9%E7%AE%97%E6%B3%95%E7%90%86%E8%A7%A3/</url>
    <content><![CDATA[<html><head></head><body><p>角点的定义：角点检测算法基本思想是使用一个固定窗口（取某个像素的一个邻域窗口）在图像上进行<strong>任意方向</strong>上的滑动，比较滑动前与滑动后两种情况，窗口中的像素灰度变化程度，<strong>如果存在任意方向上的滑动，都有着较大灰度变化，那么我们可以认为该窗口中存在角点。</strong></p>
<p>角点：最直观的印象就是在水平和竖直两个方向变化均较大的两个点，即X,Y方向梯度都较大</p>
<p>边缘：仅在水平或竖直方向有较大的变化量，即X、Y方向梯度只有一个较大</p>
<p>平坦区域：在水平和竖直方向的变化量均较小，即X、Y方向梯度都较小</p>
<p><strong>在特征点检测中经常提出尺度不变、旋转不变、抗噪声影响等，这些是判断特征点是否稳定的指标。</strong></p>
<p><strong>性能较好的角点：</strong></p>
<ol>
<li><strong>检测出图像中“真实”的角点</strong></li>
<li><strong>准确的定位性能</strong></li>
<li><strong>很高的重复检测率</strong></li>
<li><strong>噪声的鲁棒性</strong></li>
<li><strong>较高的计算效率</strong></li>
</ol>
<p>今天看到角点检测的时候真的是一脸蒙蔽，服了，书上直接给出的公式。整的我直接懵了。参考晚上其他大佬写的博客后逐渐明白了一点</p>
<p>书上给的公式如下：<br>$$<br>E(u,v)=\sum_{x,y}w(x,y) \left[]<br> \begin{matrix}<br>  Ix^2 & IxIy\<br>  IxIy & Iy^2<br>  \end{matrix}<br>  \right] \tag{B}<br>$$</p>
<p>w(x,y)是窗口函数，最简单情形就是窗口WW内的所有像素所对应的w权重系数均为1.但有时候，我们会将w(x,y)函数设置为以窗口WW中心为原点的二元正态分布。如果窗口WW中心点是角点时，移动前与移动后，该点在灰度变化贡献最大；而离窗口WW中心(角点)较远的点，这些点的灰度变化几近平缓，这些点的权重系数，可以设定小值，以示该点对灰度变化贡献较小，那么我们自然而然想到使用二元高斯函数来表示窗口函数；</p>
<p><img src="/.com//F:%5C%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95%5C1.png" alt></p>
<p>具体推导过程为:</p>
<p>使用一个固定窗口在图像上进行任意方向上的滑动。滑动分别为u和v.</p>
<p>比较滑动前与滑动后俩种情况。所以可以得到以下公式：<br>$$<br>E(u,v)=\sum_{x,y}w(x,y)[I(x+u,y+v)-I(x,y)]^2<br>$$</p>
<p>   高数中将这个式子进行泰勒展开：<br>$$<br>f(x+u,y+v)\approx f(x,y)+ufx(x,y)+vfy(x,y)\<br>所以\sum[I(x+u,y+v)-I(x,y)]^2=\sum[I(x,y)+uIx+vIy-I(x,y)]^2\<br>=\sum(u^2Ix^2)+2uvIxIy+v^2Iy^2=\sum[u,v] \left[<br> \begin{matrix}<br>  Ix^2 & IxIy\<br>  IxIy & Iy^2<br>  \end{matrix}<br>  \right] \tag{B}<br>  \left[<br>  \begin{matrix}<br>  u\<br>  v\<br>  \end{matrix}<br>  \right]<br>$$</p>
<ul>
<li>[u,v][u,v]是窗口WW的偏移量；</li>
<li>(x,y)(x,y)是窗口WW所对应的像素坐标位置，窗口有多大，就有多少个位置；</li>
<li>I(x,y)是像素坐标位置(x,y)的图像灰度值；</li>
<li>I(x+u,y+v)是像素坐标位置(x+u,y+v)的图像灰度值；</li>
</ul>
<p> Ix,Iy分别为窗口内像素点(x,y)在x方向上和y方向上的梯度值。  其中<strong>窗口函数（权重矩阵）</strong>可以是平坦的，通常为高斯滤波器Gσ：</p>
<p>设:<br>$$<br>Mi =\left[<br> \begin{matrix}<br>  Ix^2 & IxIy\<br>  IxIy & Iy^2<br>  \end{matrix}<br>  \right]\<br>  M = \sum_{x,y}w(x,y) * Mi<br>$$</p>
<p>所以可以得到卷积：M</p>
<p>该卷积的目的是得到MI在周围像素上的局部平均。<strong>矩阵M又称为Harris矩阵。</strong>W 的宽度决定了在像素x 周围的感兴趣区域。</p>
<p>根据上述表达式，当窗口在平坦区域上移动，可以想象得到，灰度不会发生什么变换。E(u,v)=0；如果窗口处在纹理比较丰富的区域上滑动，那么灰度变化会很大。算法最终思想就是计算灰度发生较大变化时所对应的位置，当然这个较大是指任意方向上的滑动，并非单指某个方向。</p>
<p>接下来我们对矩阵M的特征值进行分析。设矩阵M的特征值为λ1,λ2.</p>
<p>则有以下三种情况：</p>
<p><img src="/.com//F:%5C%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95%5Charris1.png" alt></p>
<p>具体为什么有这三种情况以下就是推导过程：</p>
<p>我们可以将E(u,v)近似为二项函数：</p>
<p>​<br>$$<br>E(u,v)=Au^2+2Cuv+Bv^2=\left[<br> \begin{matrix}<br>  A & C\<br>  C & A\<br>  \end{matrix}<br>  \right]\<br>A=\sum_{x,y}w(x,y)<em>Ix^2\<br>B=\sum_{x,y}w(x,y)</em>Iy^2\<br>C=\sum_{x,y}w(x,y)<em>IxIy<br>$$<br>二次项函数本质上就是一个椭圆函数。椭圆的长和宽是由MM的特征值λ1,λ2决定的(椭圆的长短轴正是矩阵MM特征值平方根的倒数)，椭圆的方向是由M的特征向量决定的，椭圆方程为：<br>$$<br>\left[<br> \begin{matrix}<br>  u & v\<br>  \end{matrix}<br>  \right]</em>M*\left[<br> \begin{matrix}<br>   u\<br>   v\<br>  \end{matrix}<br>  \right]=1<br>$$<br>虽然我们利用E(u,v)来描述角点的基本思想，然而最终我们仅仅使用的是矩阵M。让我们看看矩阵M形式，是不是跟协方差矩阵形式很像，像归像，但是还是有些不同，哪儿不同？一般协方差矩阵对应维的随机变量需要减去该维随机变量的均值：</p>
<p>把Ix,Iy看成两个字段，假设窗口内有m个像素点，也就是等价于有m个样本，我们先计算每个字段的均值：<br>$$<br>\overline{Ix}=\sum_{i}^mIxi\<br>\overline{Iy}=\sum_{i}^mIyi<br>$$<br>我们仍然使用(Ixi,Iyi)表示样本(Ixi,Iyi)去均值后的值，则由这m个样本组成的矩阵：<br>$$<br>x=\left[<br> \begin{matrix}<br>   Ix1 & Ix2 & … & Ixm \<br>   Iy1 & Iy2 & … & Iym\<br>  \end{matrix}<br>  \right]<br>$$<br>则对应协方差矩阵可以写成：<br>$$<br>C=\frac{1}{m}<em>X</em>X^T=\frac{1}{m}*\sum_{i=1}\left[<br> \begin{matrix}<br>  Ix^2 & IxIy\<br>  IxIy & Iy^2<br>  \end{matrix}<br>  \right]<br>$$<br>但矩阵M中并没有这样做，所以在矩阵M里，我们先进行各维的均值化处理，那么各维所对应的随机变量的均值为0，协方差矩阵就大大简化了，简化的最终结果就是矩阵M，是否明白了(注意为了简化运算，我们先假设M矩阵中的权重系数w(x,y)=1，并且省略掉了求均值.我们的目的是分析数据的主要成分</p>
<p>如果我们对协方差矩阵M进行对角化，很明显，其对角线就是各个字段的方差，这点大家应该明白吧？不明白的话可以复习下PCA原理。</p>
<ul>
<li>如果两个字段(Ix,Iy)所对应的特征值都比较大，说明什么？ 像素点的梯度分布比较散，梯度变化程度比较大，符合角点在窗口区域的特点；</li>
<li>如果是平坦区域，那么像素点的梯度所构成的点集比较集中在原点附近，因为窗口区域内的像素点的梯度幅值非常小，此时矩阵M的对角化的两个特征值比较小；</li>
<li>如果是边缘区域，在计算像素点的x、y方向上的梯度时，边缘上的像素点的某个方向的梯度幅值变化比较明显，另一个方向上的梯度幅值变化较弱，其余部分的点都还是集中原点附近，这样MM对角化后的两个特征值理论应该是一个比较大，一个比较小，当然对于边缘这种情况，可能是呈45°的边缘，致使计算出的特征值并不是都特别的大，总之跟含有角点的窗口的分布情况还是不同的。</li>
</ul>
<p>因此可以得出下列结论：</p>
<ul>
<li><p>特征值都比较大时，即窗口中含有角点；</p>
</li>
<li><p>特征值一个较大，一个较小，窗口中含有边缘；</p>
</li>
<li><p>特征值都比较小，窗口处在平坦区域；</p>
<p>那么根据书上又提出了一个不用实际计算特征值的方法。也就是度量角点响应：</p>
<p>对每一个窗口计算得到一个分数R，根据R的大小来判定窗口内是否存在harris特征角。分数R根据下面公式计算得到：<br>$$<br>R=det(M)-k(trace(M))^2\<br>其中：det(M)=\lambda1*\lambda2\<br>trace(M)=\lambda1+\lambda2\<br>另外我在别的地方看到简化后的R为：<br>R=[(Ix^2+Iy^2)-(IxIy)^2]/(Ix^2+Iy^2)<br>$$</p>
</li>
</ul>
<p>所以得到Harris角点的步骤为：</p>
<p>1、计算图像I(x,y)I(x,y)在x和y两个方向的梯度Ix,Iy;</p>
<p>2、计算图像两个方向梯度的乘积;</p>
<p>3、使用高斯函数对第二步算出的数进行高斯加权，计算中心点为(x,y)的窗口W对应的矩阵M</p>
<p>4、计算每个像素点(x,y)处的(x,y)处的Harris响应值R；</p>
<p>5：过滤大于某一阈值t的R值；</p>
<p>python代码如下：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scipy.ndimage <span class="keyword">import</span> filters</span><br><span class="line"><span class="keyword">from</span> PIL <span class="keyword">import</span> Image</span><br><span class="line"><span class="keyword">from</span> pylab <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> imtools <span class="keyword">import</span> imresize</span><br><span class="line">np.set_printoptions(threshold=np.inf)<span class="comment">#解决printf打印矩阵不完全的问题</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">harris_response</span><span class="params">(im,sigma=<span class="number">3</span>)</span>:</span></span><br><span class="line">    <span class="string">"""计算图像的harris响应函数"""</span></span><br><span class="line">    <span class="comment">#计算导数</span></span><br><span class="line">    imx = zeros(im.shape)</span><br><span class="line">    imx = filters.gaussian_filter(im,(sigma,sigma),(<span class="number">0</span>,<span class="number">1</span>),imx)</span><br><span class="line">    imy = zeros(im.shape)</span><br><span class="line">    imy = filters.gaussian_filter(im, (sigma, sigma), (<span class="number">1</span>, <span class="number">0</span>), imy)</span><br><span class="line">    <span class="comment">#计算Harris的各个分量</span></span><br><span class="line">    wxx = filters.gaussian_filter(imx*imx,sigma)</span><br><span class="line">    wxy = filters.gaussian_filter(imx*imy,sigma)</span><br><span class="line">    wyy = filters.gaussian_filter(imy*imy,sigma)</span><br><span class="line">    <span class="comment">#计算像素的角点响应函数</span></span><br><span class="line">    <span class="keyword">return</span> (wxx*wyy - <span class="number">2</span>*wxy)/(wxx + wyy)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_harris_points</span><span class="params">(harrism,min_dist = <span class="number">10</span>,thresold = <span class="number">0.1</span>)</span>:</span></span><br><span class="line">    <span class="string">"""从一幅Harrisim响应中返回角点，min_dist为分割角点和图像边界的最少像素数目"""</span></span><br><span class="line">    corner_thsold = harrism.max()*thresold</span><br><span class="line">    harrism_t = (harrism > corner_thsold) * <span class="number">1</span></span><br><span class="line">    <span class="comment">#得到候选点的坐标</span></span><br><span class="line">    coords = array(harrism_t.nonzero()).T<span class="comment">#返回非零值的坐标的矩阵</span></span><br><span class="line">    <span class="comment">#他们的Harris响应值</span></span><br><span class="line">    candidate_values = [harrism[c[<span class="number">0</span>],c[<span class="number">1</span>]] <span class="keyword">for</span> c <span class="keyword">in</span> coords]</span><br><span class="line">    <span class="comment">#对候选点进行harris响应值进行排序</span></span><br><span class="line">    index = argsort(candidate_values)[::<span class="number">-1</span>]<span class="comment">#将x中的元素从小到大排列，提取其对应的index(索引)，然后输出到y</span></span><br><span class="line">    <span class="comment">#将可行点的位置保存在数组里</span></span><br><span class="line">    allowed_locations = zeros(harrism.shape)</span><br><span class="line">    allowed_locations[min_dist:-min_dist,min_dist:-min_dist] = <span class="number">1</span></span><br><span class="line">    <span class="comment">#按照min_distance原则，选择最佳harris点</span></span><br><span class="line">    filters_coords = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> index:</span><br><span class="line">            <span class="keyword">if</span> allowed_locations[coords[i,<span class="number">0</span>],coords[i,<span class="number">1</span>]] == <span class="number">1</span>:</span><br><span class="line">                filters_coords.append(coords[i])</span><br><span class="line">                allowed_locations[(coords[i,<span class="number">0</span>]-min_dist):(coords[i,<span class="number">0</span>]+min_dist),(coords[i,<span class="number">1</span>]-min_dist):coords[i,<span class="number">1</span>]+min_dist] = <span class="number">0</span></span><br><span class="line">    <span class="keyword">return</span> filters_coords</span><br></pre></td></tr></tbody></table></figure>

</body></html>]]></content>
      <categories>
        <category>角点描述</category>
      </categories>
      <tags>
        <tag>Harris</tag>
      </tags>
  </entry>
</search>
